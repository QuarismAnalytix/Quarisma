name: XSigma CI - Streamlined Testing Pipeline

# This CI pipeline provides comprehensive testing for XSigma across multiple platforms and configurations.
# Key features:
# - C++17 baseline testing across all platforms (Ubuntu, Windows, macOS)
# - Selective C++20/C++23 testing with specific TBB/CUDA combinations
# - TBB support for Unix/macOS only (Windows TBB tested separately with Visual Studio)
# - CUDA integration across all platforms where supported
# - Sanitizer testing restricted to Unix-based systems for better compatibility
# - Streamlined job matrix to reduce CI time while maintaining comprehensive coverage

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]
  workflow_dispatch:

env:
  BUILD_TYPE: Release
  CACHE_VERSION: v3  # v3: invalidate caches from the Hisab repo rename (absolute CMakeCache paths)
  SCCACHE_VERSION: "0.7.7"  # Version of sccache to use
  BUILDCACHE_VERSION: "0.28.4"  # Version of buildcache to use on Windows

jobs:
  # ============================================================================
  # Primary Build Matrix - C++17 Baseline Testing
  # Tests core functionality with C++17 across all platforms and build types
  # Windows: No TBB (tested separately), CUDA enabled where available
  # Unix/macOS: Both TBB enabled/disabled, CUDA enabled where available
  # ============================================================================
  build-matrix:
    name: ${{ matrix.name }}
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        include:
          # ===== C++17 Baseline Testing (Primary) =====
          # Ubuntu C++17 - Debug and Release with TBB variants
          - name: "Ubuntu C++17 Debug - LOGURU - TBB:ON"
            os: ubuntu-latest
            build_type: Debug
            cxx_std: 17
            logging_backend: LOGURU
            tbb_enabled: ON
            cuda_enabled: OFF
            compiler_c: clang
            compiler_cxx: clang
            generator: Ninja
            cache_path: ~/.cache

          - name: "Ubuntu C++17 Debug - NATIVE - TBB:OFF"
            os: ubuntu-latest
            build_type: Debug
            cxx_std: 17
            logging_backend: NATIVE
            tbb_enabled: OFF
            cuda_enabled: OFF
            compiler_c: clang
            compiler_cxx: clang
            generator: Ninja
            cache_path: ~/.cache

          - name: "Ubuntu C++17 Release - GLOG - TBB:ON"
            os: ubuntu-latest
            build_type: Release
            cxx_std: 17
            logging_backend: GLOG
            tbb_enabled: ON
            cuda_enabled: OFF
            compiler_c: clang
            compiler_cxx: clang
            generator: Ninja
            cache_path: ~/.cache

          # Windows C++17 - Debug and Release (no TBB, CUDA where available)
          # Temporarily disabled - CUDA testing
          # - name: "Windows C++17 Debug - LOGURU - CUDA:ON"
          #   os: windows-latest
          #   build_type: Debug
          #   cxx_std: 17
          #   logging_backend: LOGURU
          #   tbb_enabled: OFF
          #   cuda_enabled: ON
          #   compiler_c: clang
          #   compiler_cxx: clang
          #   generator: Ninja
          #   cache_path: ~/AppData/Local/Temp/chocolatey

          - name: "Windows C++17 Release - NATIVE - TBB:OFF"
            os: windows-latest
            build_type: Release
            cxx_std: 17
            logging_backend: NATIVE
            tbb_enabled: OFF
            cuda_enabled: OFF
            compiler_c: clang
            compiler_cxx: clang
            generator: Ninja
            cache_path: ~/AppData/Local/Temp/chocolatey

          - name: "Windows C++17 Debug - LOGURU - TBB:ON"
            os: windows-latest
            build_type: Debug
            cxx_std: 17
            logging_backend: LOGURU
            tbb_enabled: ON
            cuda_enabled: OFF
            compiler_c: clang
            compiler_cxx: clang
            generator: Ninja
            cache_path: ~/AppData/Local/Temp/chocolatey

          - name: "Windows C++20 Release - LOGURU - TBB:OFF"
            os: windows-latest
            build_type: Release
            cxx_std: 20
            logging_backend: LOGURU
            tbb_enabled: OFF
            cuda_enabled: OFF
            compiler_c: clang
            compiler_cxx: clang
            generator: Ninja
            cache_path: ~/AppData/Local/Temp/chocolatey

          - name: "Ubuntu C++17 Release - GCC - TBB:ON"
            os: ubuntu-latest
            build_type: Release
            cxx_std: 17
            logging_backend: LOGURU
            tbb_enabled: ON
            cuda_enabled: OFF
            compiler_c: gcc
            compiler_cxx: gcc
            generator: Ninja
            cache_path: ~/.cache

          - name: "Ubuntu C++17 Release - SPDLOG - TBB:ON"
            os: ubuntu-latest
            build_type: Release
            cxx_std: 17
            logging_backend: SPDLOG
            tbb_enabled: ON
            cuda_enabled: OFF
            compiler_c: clang
            compiler_cxx: clang
            generator: Ninja
            cache_path: ~/.cache

          # macOS C++17 - Debug and Release with TBB variants
          - name: "macOS C++17 Debug - LOGURU - TBB:ON"
            os: macos-latest
            build_type: Debug
            cxx_std: 17
            logging_backend: LOGURU
            tbb_enabled: ON
            cuda_enabled: OFF  # CUDA not officially supported on Apple Silicon
            compiler_c: clang
            compiler_cxx: clang
            generator: Ninja
            cache_path: ~/Library/Caches

          - name: "macOS C++17 Release - GLOG - TBB:OFF"
            os: macos-latest
            build_type: Release
            cxx_std: 17
            logging_backend: GLOG
            tbb_enabled: OFF
            cuda_enabled: OFF
            compiler_c: clang
            compiler_cxx: clang
            generator: Ninja
            cache_path: ~/Library/Caches

          # ===== C++20 Targeted Testing (Release only) =====
          - name: "Ubuntu C++20 Release - TBB:ON"
            os: ubuntu-latest
            build_type: Release
            cxx_std: 20
            logging_backend: LOGURU
            tbb_enabled: ON
            cuda_enabled: OFF
            compiler_c: clang
            compiler_cxx: clang
            generator: Ninja
            cache_path: ~/.cache

          # Temporarily disabled - CUDA testing
          # - name: "Ubuntu C++20 Release - CUDA:ON"
          #   os: ubuntu-latest
          #   build_type: Release
          #   cxx_std: 20
          #   logging_backend: NATIVE
          #   tbb_enabled: OFF
          #   cuda_enabled: ON
          #   compiler_c: clang
          #   compiler_cxx: clang
          #   generator: Ninja
          #   cache_path: ~/.cache

          # Temporarily disabled - CUDA testing
          # - name: "Windows C++20 Release - CUDA:ON"
          #   os: windows-latest
          #   build_type: Release
          #   cxx_std: 20
          #   logging_backend: LOGURU
          #   tbb_enabled: OFF
          #   cuda_enabled: ON
          #   compiler_c: clang
          #   compiler_cxx: clang
          #   generator: Ninja
          #   cache_path: ~/AppData/Local/Temp/chocolatey

          - name: "macOS C++20 Release - TBB:ON"
            os: macos-latest
            build_type: Release
            cxx_std: 20
            logging_backend: LOGURU
            tbb_enabled: ON
            cuda_enabled: OFF  # CUDA not supported on Apple Silicon
            compiler_c: clang
            compiler_cxx: clang
            generator: Ninja
            cache_path: ~/Library/Caches

          # ===== C++23 Targeted Testing (Release only) =====
          - name: "Ubuntu C++23 Release - TBB:ON"
            os: ubuntu-latest
            build_type: Release
            cxx_std: 23
            logging_backend: GLOG
            tbb_enabled: ON
            cuda_enabled: OFF
            compiler_c: clang
            compiler_cxx: clang
            generator: Ninja
            cache_path: ~/.cache

          # Temporarily disabled - CUDA testing
          # - name: "Windows C++23 Release - CUDA:ON"
          #   os: windows-latest
          #   build_type: Release
          #   cxx_std: 23
          #   logging_backend: NATIVE
          #   tbb_enabled: OFF
          #   cuda_enabled: ON
          #   compiler_c: clang
          #   compiler_cxx: clang
          #   generator: Ninja
          #   cache_path: ~/AppData/Local/Temp/chocolatey

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        submodules: recursive
        fetch-depth: 0

    - name: Cache dependencies
      uses: actions/cache@v4
      with:
        path: |
          ${{ matrix.cache_path }}
          build/ThirdParty
        key: ${{ runner.os }}-deps-${{ env.CACHE_VERSION }}-${{ hashFiles('**/CMakeLists.txt', 'ThirdParty/**') }}
        restore-keys: |
          ${{ runner.os }}-deps-${{ env.CACHE_VERSION }}-

    # ===== Install Dependencies Using Scripts =====
    - name: Install dependencies (Ubuntu)
      if: runner.os == 'Linux'
      run: |
        chmod +x .github/workflows/install/install-deps-ubuntu.sh
        ./.github/workflows/install/install-deps-ubuntu.sh --with-tbb

    - name: Install dependencies (macOS)
      if: runner.os == 'macOS'
      run: |
        chmod +x .github/workflows/install/install-deps-macos.sh
        ./.github/workflows/install/install-deps-macos.sh --with-tbb

    - name: Install dependencies (Windows)
      if: runner.os == 'Windows'
      shell: pwsh
      run: |
        if ("${{ matrix.tbb_enabled }}" -eq "ON") {
          & .\.github\workflows\install\install-deps-windows.ps1 -WithTbb
        } else {
          & .\.github\workflows\install\install-deps-windows.ps1
        }

    - name: Setup macOS linker environment
      if: runner.os == 'macOS'
      run: |
        # Set up linker flags for Homebrew LLVM libc++
        # This fixes the missing std::__1::__hash_memory symbol issue
        echo "LDFLAGS=-L/opt/homebrew/opt/llvm/lib/c++ -L/opt/homebrew/opt/llvm/lib -Wl,-rpath,/opt/homebrew/opt/llvm/lib/c++ -Wl,-rpath,/opt/homebrew/opt/llvm/lib" >> $GITHUB_ENV
        echo "CPPFLAGS=-I/opt/homebrew/opt/llvm/include" >> $GITHUB_ENV
        echo "/opt/homebrew/opt/llvm/bin" >> $GITHUB_PATH

    # Temporarily disabled - CUDA testing
    # - name: Install CUDA Toolkit (Windows)
    #   if: runner.os == 'Windows' && matrix.cuda_enabled == 'ON'
    #   shell: pwsh
    #   run: |
    #     # Install NVIDIA CUDA Toolkit for Windows
    #     Write-Host "=== Installing CUDA Toolkit for Windows ==="
    #
    #     # Try chocolatey installation first
    #     Write-Host "Attempting CUDA installation via chocolatey..."
    #     try {
    #       choco install cuda --version=12.3.0 -y --no-progress --timeout=600
    #       if ($LASTEXITCODE -eq 0) {
    #         Write-Host "SUCCESS: CUDA Toolkit installed via chocolatey"
    #
    #         # Verify installation
    #         $nvccPath = Get-Command nvcc -ErrorAction SilentlyContinue
    #         if ($nvccPath) {
    #           Write-Host "CUDA compiler found at: $($nvccPath.Source)"
    #           & nvcc --version
    #         } else {
    #           Write-Host "WARNING: nvcc not found in PATH after installation"
    #         }
    #       } else {
    #         throw "Chocolatey installation returned non-zero exit code"
    #       }
    #     } catch {
    #       Write-Host "WARNING: CUDA installation via chocolatey failed: $_"
    #       Write-Host "Attempting direct download from NVIDIA..."
    #
    #       try {
    #         # Fallback: Direct download from NVIDIA
    #         $cudaUrl = "https://developer.download.nvidia.com/compute/cuda/12.3.0/network_installers/cuda_12.3.0_windows_network.exe"
    #         $installerPath = "$env:TEMP\cuda_installer.exe"
    #
    #         Write-Host "Downloading CUDA installer..."
    #         Invoke-WebRequest -Uri $cudaUrl -OutFile $installerPath -TimeoutSec 300
    #
    #         Write-Host "Running CUDA installer (silent mode)..."
    #         Start-Process -FilePath $installerPath -ArgumentList "-s" -Wait -NoNewWindow
    #
    #         if ($LASTEXITCODE -eq 0) {
    #           Write-Host "SUCCESS: CUDA installed via direct download"
    #         } else {
    #           throw "Direct installer returned non-zero exit code"
    #         }
    #       } catch {
    #         Write-Host "WARNING: Direct CUDA installation also failed: $_"
    #         Write-Host "Build will continue without CUDA support"
    #         Write-Host "This is non-fatal - CUDA is optional for this build configuration"
    #       }
    #     }
    #
    #     # Final verification
    #     Write-Host "`n=== CUDA Installation Summary ==="
    #     if (Get-Command nvcc -ErrorAction SilentlyContinue) {
    #       Write-Host "Status: CUDA available"
    #       & nvcc --version
    #     } else {
    #       Write-Host "Status: CUDA not available (build will continue without CUDA)"
    #     }

    - name: Install Intel MKL (Windows)
      if: runner.os == 'Windows'
      shell: pwsh
      run: |
        # Intel MKL installation on Windows
        Write-Host "Intel MKL installation for Windows..."
        Write-Host "Note: Intel MKL can be installed via Intel oneAPI installer or vcpkg"
        Write-Host "For CI purposes, using system math libraries"
        Write-Host "To install MKL: vcpkg install intel-mkl:x64-windows (if available)"
        Write-Host "Or download Intel oneAPI Base Toolkit from Intel website"

    # ===== Python Setup =====
    - name: Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.x'
        cache: 'pip'

    - name: Install Python dependencies
      run: |
        python -m pip install --upgrade pip
        pip install psutil==6.1.1 colorama==0.4.6

    # ===== Verify Code Quality Tools Installation =====
    - name: Verify code quality tools (Unix only)
      if: runner.os != 'Windows'
      run: |
        echo "=== Verifying Code Quality Tools Installation ==="
        if [ "${{ runner.os }}" = "Linux" ]; then
          echo "Valgrind version:"
          valgrind --version || echo "Valgrind not available"
          echo ""
          echo "NUMA library check:"
          ldconfig -p | grep numa || echo "NUMA libraries not found"
          echo ""
        fi
        echo "Cppcheck version:"
        cppcheck --version || echo "Cppcheck not available"
        echo ""
        echo "Tools verification complete"

    # ===== Build Configuration and Execution Using CMake =====
    - name: Export Unix/macOS build environment
      if: runner.os != 'Windows'
      run: |
        if [ "${{ matrix.compiler_cxx }}" = "clang" ]; then
          echo "CC_COMPILER=clang" >> "$GITHUB_ENV"
          echo "CXX_COMPILER=clang++" >> "$GITHUB_ENV"
        else
          echo "CC_COMPILER=${{ matrix.compiler_cxx }}" >> "$GITHUB_ENV"
          echo "CXX_COMPILER=g++" >> "$GITHUB_ENV"
        fi
        BUILD_DIR="build_ninja"
        if [ "${{ matrix.tbb_enabled }}" = "ON" ]; then
          BUILD_DIR="${BUILD_DIR}_tbb"
        fi
        if [ "${{ matrix.cxx_std }}" != "17" ]; then
          BUILD_DIR="${BUILD_DIR}_cxx${{ matrix.cxx_std }}"
        fi
        echo "BUILD_DIR=$BUILD_DIR" >> "$GITHUB_ENV"

    - name: Configure (Unix/macOS)
      if: runner.os != 'Windows'
      run: |
        cmake -B "$BUILD_DIR" \
          -S . \
          -G Ninja \
          -DCMAKE_BUILD_TYPE=${{ matrix.build_type }} \
          -DCMAKE_C_COMPILER=$CC_COMPILER \
          -DCMAKE_CXX_COMPILER=$CXX_COMPILER \
          -DCMAKE_CXX_STANDARD=${{ matrix.cxx_std }} \
          -DLOGGING_BACKEND=${{ matrix.logging_backend }} \
          -DBUILD_TESTING=ON \
          -DPARALLEL_ENABLE_TBB=${{ matrix.tbb_enabled }} \
          -DMEMORY_ENABLE_TBB=${{ matrix.tbb_enabled }} \
          -DMEMORY_GPU_BACKEND=${{ matrix.cuda_enabled == 'ON' && 'cuda' || 'none' }}

    - name: Build (Unix/macOS)
      if: runner.os != 'Windows'
      run: cmake --build "$BUILD_DIR" -j 2

    - name: Test (Unix/macOS)
      if: runner.os != 'Windows'
      run: |
        # Vendored libsvml.so needs libintlc.so.5; the .so has no RPATH, so
        # GCC (which auto-enables ThirdParty SVML) fails without this.
        if [ -f ThirdParty/svml/unix/lib/libintlc.so.5 ]; then
          export LD_LIBRARY_PATH="${PWD}/ThirdParty/svml/unix/lib:${BUILD_DIR}/lib:${LD_LIBRARY_PATH:-}"
        fi
        ctest --test-dir "$BUILD_DIR" --output-on-failure -j 2

    - name: Configure and Build (Windows)
      if: runner.os == 'Windows'
      shell: pwsh
      run: |
        # Determine build directory name
        $buildDir = "build_ninja"
        if ("${{ matrix.tbb_enabled }}" -eq "ON") {
          $buildDir = "$buildDir`_tbb"
        }
        if ("${{ matrix.cxx_std }}" -ne "17") {
          $buildDir = "$buildDir`_cxx${{ matrix.cxx_std }}"
        }

        # Configure CMake
        cmake -B "$buildDir" `
          -S . `
          -G Ninja `
          -DCMAKE_BUILD_TYPE=${{ matrix.build_type }} `
          -DCMAKE_C_COMPILER=clang `
          -DCMAKE_CXX_COMPILER=clang++ `
          -DCMAKE_CXX_STANDARD=${{ matrix.cxx_std }} `
          -DLOGGING_BACKEND=${{ matrix.logging_backend }} `
          -DBUILD_TESTING=ON `
          -DPARALLEL_ENABLE_TBB=${{ matrix.tbb_enabled }} `
          -DMEMORY_ENABLE_TBB=${{ matrix.tbb_enabled }} `
          -DMEMORY_GPU_BACKEND=${{ matrix.cuda_enabled == 'ON' && 'cuda' || 'none' }}
        if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

        # Build
        cmake --build "$buildDir" -j 2
        if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

        # Run tests
        ctest --test-dir "$buildDir" --output-on-failure -j 2
        if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

    - name: Verify Build Configuration
      shell: bash
      run: |
        # Determine build directory name (same logic as build steps)
        BUILD_DIR="build_ninja"
        if [ "${{ matrix.tbb_enabled }}" = "ON" ]; then
          BUILD_DIR="${BUILD_DIR}_tbb"
        fi
        if [ "${{ matrix.cxx_std }}" != "17" ]; then
          BUILD_DIR="${BUILD_DIR}_cxx${{ matrix.cxx_std }}"
        fi

        echo "=== Build Configuration Verification ==="
        echo "Platform: ${{ runner.os }}"
        echo "Build Directory: $BUILD_DIR"
        echo "C++ Standard: ${{ matrix.cxx_std }}"
        echo "Build Type: ${{ matrix.build_type }}"
        echo "Logging Backend: ${{ matrix.logging_backend }}"
        echo "TBB Enabled: ${{ matrix.tbb_enabled }}"
        echo "CUDA Enabled: ${{ matrix.cuda_enabled }}"
        echo ""

        # Verify core library was built successfully
        if [ "${{ runner.os }}" = "Linux" ]; then
          if [ -f "$BUILD_DIR/Library/Core/libCore.so" ] || [ -f "$BUILD_DIR/Library/Core/libCore.a" ]; then
            echo "SUCCESS: Core library built successfully (Linux)"
            # Check TBB linkage if enabled
            if [ "${{ matrix.tbb_enabled }}" = "ON" ] && [ -f "$BUILD_DIR/Library/Core/libCore.so" ]; then
              if ldd $BUILD_DIR/Library/Core/libCore.so | grep -q "tbb"; then
                echo "SUCCESS: TBB dependencies found in Core library"
              else
                echo "INFO: TBB may be statically linked or not found"
              fi
            fi
          else
            echo "WARNING: Core library not found (Linux)"
          fi
        elif [ "${{ runner.os }}" = "macOS" ]; then
          if [ -f "$BUILD_DIR/Library/Core/libCore.dylib" ] || [ -f "$BUILD_DIR/Library/Core/libCore.a" ]; then
            echo "SUCCESS: Core library built successfully (macOS)"
            # Check TBB linkage if enabled
            if [ "${{ matrix.tbb_enabled }}" = "ON" ] && [ -f "$BUILD_DIR/Library/Core/libCore.dylib" ]; then
              if otool -L $BUILD_DIR/Library/Core/libCore.dylib | grep -q "tbb"; then
                echo "SUCCESS: TBB dependencies found in Core library"
              else
                echo "INFO: TBB may be statically linked or not found"
              fi
            fi
          else
            echo "WARNING: Core library not found (macOS)"
          fi
        elif [ "${{ runner.os }}" = "Windows" ]; then
          if [ -f "$BUILD_DIR/Library/Core/Core.dll" ] || [ -f "$BUILD_DIR/Library/Core/Core.lib" ]; then
            echo "SUCCESS: Core library built successfully (Windows)"
            echo "INFO: Windows TBB testing is handled separately with Visual Studio"
          else
            echo "WARNING: Core library not found (Windows)"
          fi
        fi

    - name: Prepare test results artifact name
      if: failure()
      shell: bash
      run: |
        # upload-artifact rejects: " : < > | * ? CR LF \ /
        raw='${{ matrix.name }}'
        safe=$(printf '%s' "$raw" | tr -d '\015\012' | tr ':"<>|*?/' '-' | tr '\\' '-' | tr '"' '-' | tr -s '-')
        echo "TEST_RESULTS_ARTIFACT_NAME=test-results-${safe}" >> "$GITHUB_ENV"

    - name: Upload test results
      if: failure()
      uses: actions/upload-artifact@v4
      with:
        name: ${{ env.TEST_RESULTS_ARTIFACT_NAME }}
        path: |
          build*/Testing/Temporary/LastTest.log
          build*/**/*.log

  # ============================================================================
  # TBB-Specific Testing - Unix/macOS Only
  # Focused TBB functionality tests for platforms where TBB is officially supported
  # Windows TBB testing is handled separately with Visual Studio builds
  # ============================================================================
  tbb-specific-tests:
    name: TBB Functionality - ${{ matrix.os }} - ${{ matrix.build_type }}
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]  # Windows removed - TBB tested separately
        build_type: [Debug, Release]

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        submodules: recursive

    - name: Cache dependencies
      uses: actions/cache@v4
      with:
        path: |
          ~/.cache
          ~/Library/Caches
          build/ThirdParty
        key: ${{ runner.os }}-tbb-${{ env.CACHE_VERSION }}-${{ hashFiles('**/CMakeLists.txt') }}
        restore-keys: |
          ${{ runner.os }}-tbb-${{ env.CACHE_VERSION }}-

    # ===== Ubuntu TBB Dependencies =====
    - name: Install dependencies (Ubuntu)
      if: runner.os == 'Linux'
      run: |
        sudo apt-get update
        sudo apt-get install -y \
          build-essential \
          ninja-build \
          clang \
          llvm \
          cmake \
          python3 \
          python3-pip \
          libtbb-dev \
          valgrind \
          cppcheck \
          libnuma-dev \
          sccache

    - name: Install Intel TBB (Ubuntu)
      if: runner.os == 'Linux'
      run: |
        wget -O- https://apt.repos.intel.com/intel-gpg-keys/GPG-PUB-KEY-INTEL-SW-PRODUCTS.PUB | gpg --dearmor | sudo tee /usr/share/keyrings/oneapi-archive-keyring.gpg > /dev/null
        echo "deb [signed-by=/usr/share/keyrings/oneapi-archive-keyring.gpg] https://apt.repos.intel.com/oneapi all main" | sudo tee /etc/apt/sources.list.d/oneAPI.list
        sudo apt-get update
        sudo apt-get install -y intel-oneapi-tbb-devel

    # ===== macOS TBB Dependencies =====
    - name: Install dependencies (macOS)
      if: runner.os == 'macOS'
      run: |
        brew install ninja llvm cmake python3 tbb cppcheck sccache

    - name: Setup macOS linker environment (TBB tests)
      if: runner.os == 'macOS'
      run: |
        # Set up linker flags for Homebrew LLVM libc++
        echo "LDFLAGS=-L/opt/homebrew/opt/llvm/lib/c++ -L/opt/homebrew/opt/llvm/lib -Wl,-rpath,/opt/homebrew/opt/llvm/lib/c++ -Wl,-rpath,/opt/homebrew/opt/llvm/lib" >> $GITHUB_ENV
        echo "CPPFLAGS=-I/opt/homebrew/opt/llvm/include" >> $GITHUB_ENV
        echo "/opt/homebrew/opt/llvm/bin" >> $GITHUB_PATH

    # ===== Python Setup =====
    - name: Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.x'
        cache: 'pip'

    - name: Install Python dependencies
      run: |
        python -m pip install --upgrade pip
        pip install psutil==6.1.1 colorama==0.4.6

    # ===== CMake Configuration for TBB Testing =====
    - name: Configure with TBB enabled
      run: |
        cmake -B build \
          -C .github/cmake/configure_cache.cmake \
          -S . \
          -G Ninja \
          -DCMAKE_BUILD_TYPE=${{ matrix.build_type }} \
          -DCMAKE_C_COMPILER=clang \
          -DCMAKE_CXX_COMPILER=clang++ \
          -DCMAKE_CXX_STANDARD=17 \
          -DLOGGING_BACKEND=LOGURU \
          -DBUILD_TESTING=ON \
          -DCORE_ENABLE_BENCHMARK=OFF \
          -DMEMORY_ENABLE_BENCHMARK=OFF \
          -DLOGGING_ENABLE_BENCHMARK=OFF \
          -DVECTORIZATION_ENABLE_BENCHMARK=OFF \
          -DPARALLEL_ENABLE_BENCHMARK=OFF \
          -DPROFILER_ENABLE_BENCHMARK=OFF \
          -DPARALLEL_ENABLE_TBB=ON \
          -DMEMORY_ENABLE_TBB=ON \
          -DMEMORY_GPU_BACKEND=none

    # ===== Build and Test TBB Functionality =====
    - name: Build
      run: cmake --build build -j 2

    - name: Run TBB-specific tests
      working-directory: ${{ github.workspace }}/build
      shell: bash
      run: |
        # Run all tests with focus on TBB-related functionality
        ctest --output-on-failure -j 2

    - name: Verify TBB Integration
      working-directory: ${{ github.workspace }}/build
      shell: bash
      run: |
        echo "=== Verifying TBB Integration ==="
        echo "Platform: ${{ runner.os }}"
        echo "Build Type: ${{ matrix.build_type }}"

        # Platform-specific TBB verification
        if [ "${{ runner.os }}" = "Linux" ]; then
          if [ -f "Library/Core/libCore.so" ]; then
            echo "SUCCESS: Core library built successfully (Linux)"
            if ldd Library/Core/libCore.so | grep -q "tbb"; then
              echo "SUCCESS: TBB dependencies found in Core library"
            else
              echo "INFO: TBB may be statically linked"
            fi
          else
            echo "WARNING: Core library not found (Linux)"
          fi
        elif [ "${{ runner.os }}" = "macOS" ]; then
          if [ -f "Library/Core/libCore.dylib" ]; then
            echo "SUCCESS: Core library built successfully (macOS)"
            if otool -L Library/Core/libCore.dylib | grep -q "tbb"; then
              echo "SUCCESS: TBB dependencies found in Core library"
            else
              echo "INFO: TBB may be statically linked"
            fi
          else
            echo "WARNING: Core library not found (macOS)"
          fi
        fi

    - name: Upload TBB test results
      if: failure()
      uses: actions/upload-artifact@v4
      with:
        name: tbb-test-results-${{ matrix.os }}-${{ matrix.build_type }}
        path: |
          build/Testing/Temporary/LastTest.log
          build/**/*.log

  # ============================================================================
  # Sanitizer Testing - Unix Systems Only
  # Sanitizers work best on Unix-based systems with Clang
  # Windows sanitizer support is limited and tested separately
  # ============================================================================
  sanitizer-tests:
    name: Sanitizer ${{ matrix.sanitizer }} - ${{ matrix.os }}
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]  # Windows removed for better compatibility
        sanitizer: [address, undefined, thread]
        exclude:
          # LeakSanitizer is not supported on Apple Silicon (ARM64)
          - os: macos-latest
            sanitizer: leak
          # ThreadSanitizer is disabled on macOS: Homebrew LLVM's tsan runtime
          # crashes at process startup (SIGSEGV in __tsan::SlotLock) on current
          # macOS/Apple Silicon runners, before any test code executes.
          - os: macos-latest
            sanitizer: thread
        include:
          # Add leak sanitizer only for Ubuntu where it's supported
          - os: ubuntu-latest
            sanitizer: leak
          # MemorySanitizer is Clang-only and needs an instrumented libc++; Ubuntu only
          - os: ubuntu-latest
            sanitizer: memory

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        submodules: recursive

    - name: Cache dependencies
      uses: actions/cache@v4
      with:
        path: |
          ~/.cache
          ~/Library/Caches
          build/ThirdParty
        key: ${{ runner.os }}-sanitizer-${{ env.CACHE_VERSION }}-${{ hashFiles('**/CMakeLists.txt') }}
        restore-keys: |
          ${{ runner.os }}-sanitizer-${{ env.CACHE_VERSION }}-

    # ===== Ubuntu Sanitizer Dependencies =====
    - name: Install dependencies (Ubuntu)
      if: matrix.os == 'ubuntu-latest'
      run: |
        sudo apt-get update
        sudo apt-get install -y \
          ninja-build \
          clang \
          llvm \
          cmake \
          python3 \
          python3-pip \
          libtbb-dev \
          valgrind \
          cppcheck \
          libnuma-dev \
          sccache

    # ===== macOS Sanitizer Dependencies =====
    - name: Install dependencies (macOS)
      if: matrix.os == 'macos-latest'
      run: |
        brew install ninja cmake llvm tbb cppcheck sccache
        echo "/opt/homebrew/opt/llvm/bin" >> $GITHUB_PATH

    - name: Setup macOS linker environment (Sanitizer tests)
      if: matrix.os == 'macos-latest'
      run: |
        # Set up linker flags for Homebrew LLVM libc++
        echo "LDFLAGS=-L/opt/homebrew/opt/llvm/lib/c++ -L/opt/homebrew/opt/llvm/lib -Wl,-rpath,/opt/homebrew/opt/llvm/lib/c++ -Wl,-rpath,/opt/homebrew/opt/llvm/lib" >> $GITHUB_ENV
        echo "CPPFLAGS=-I/opt/homebrew/opt/llvm/include" >> $GITHUB_ENV

    # ===== Python Setup =====
    - name: Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.x'
        cache: 'pip'

    - name: Install Python dependencies
      run: |
        python -m pip install --upgrade pip
        pip install psutil==6.1.1 colorama==0.4.6

    # ===== CMake Configuration for Sanitizer Testing =====
    - name: Configure with ${{ matrix.sanitizer }} sanitizer
      shell: bash
      run: |
        # Use NATIVE logging backend for sanitizer builds to avoid threading/memory issues
        cmake -B build -S . -G Ninja \
          -C .github/cmake/configure_cache.cmake \
          -DCMAKE_BUILD_TYPE=Debug \
          -DCMAKE_C_COMPILER=clang \
          -DCMAKE_CXX_COMPILER=clang++ \
          -DCMAKE_CXX_STANDARD=17 \
          -DLOGGING_BACKEND=NATIVE \
          -DCORE_ENABLE_SANITIZER=ON \
          -DCORE_SANITIZER_TYPE=${{ matrix.sanitizer }} \
          -DMEMORY_ENABLE_SANITIZER=ON \
          -DMEMORY_SANITIZER_TYPE=${{ matrix.sanitizer }} \
          -DLOGGING_ENABLE_SANITIZER=ON \
          -DLOGGING_SANITIZER_TYPE=${{ matrix.sanitizer }} \
          -DVECTORIZATION_ENABLE_SANITIZER=ON \
          -DVECTORIZATION_SANITIZER_TYPE=${{ matrix.sanitizer }} \
          -DPARALLEL_ENABLE_SANITIZER=ON \
          -DPARALLEL_SANITIZER_TYPE=${{ matrix.sanitizer }} \
          -DPROFILER_ENABLE_SANITIZER=ON \
          -DPROFILER_SANITIZER_TYPE=${{ matrix.sanitizer }} \
          -DMODELS_ENABLE_SANITIZER=ON \
          -DMODELS_SANITIZER_TYPE=${{ matrix.sanitizer }} \
          -DGRAPH_ENABLE_SANITIZER=ON \
          -DGRAPH_SANITIZER_TYPE=${{ matrix.sanitizer }} \
          -DBUILD_TESTING=ON \
          -DPARALLEL_ENABLE_TBB=ON \
          -DMEMORY_ENABLE_TBB=ON \
          -DMEMORY_GPU_BACKEND=none

    - name: Build
      run: cmake --build build -j 2

    - name: Setup sanitizer environment
      shell: bash
      run: |
        # Set up sanitizer-specific environment variables
        case "${{ matrix.sanitizer }}" in
          "address")
            echo "ASAN_OPTIONS=print_stacktrace=1:check_initialization_order=1:strict_init_order=1:suppressions=${{ github.workspace }}/Scripts/suppressions/asan_suppressions.txt" >> $GITHUB_ENV
            ;;
          "leak")
            echo "LSAN_OPTIONS=print_suppressions=0:suppressions=${{ github.workspace }}/Scripts/suppressions/lsan_suppressions.txt" >> $GITHUB_ENV
            ;;
          "thread")
            echo "TSAN_OPTIONS=print_stacktrace=1:halt_on_error=1:suppressions=${{ github.workspace }}/Scripts/suppressions/tsan_suppressions.txt" >> $GITHUB_ENV
            ;;
          "undefined")
            echo "UBSAN_OPTIONS=print_stacktrace=1:halt_on_error=1:suppressions=${{ github.workspace }}/Scripts/suppressions/ubsan_suppressions.txt" >> $GITHUB_ENV
            ;;
          "memory")
            echo "MSAN_OPTIONS=print_stats=1:halt_on_error=1:suppressions=${{ github.workspace }}/Scripts/suppressions/msan_suppressions.txt" >> $GITHUB_ENV
            ;;
        esac

    - name: Run Tests with ${{ matrix.sanitizer }} Sanitizer
      working-directory: ${{ github.workspace }}/build
      run: |
        if [ "${{ matrix.sanitizer }}" = "memory" ]; then
          echo "MSAN is compile-only on hosted runners (uninstrumented libstdc++)."
          exit 0
        fi
        ctest --output-on-failure -j 2

    - name: Upload sanitizer results
      if: failure()
      uses: actions/upload-artifact@v4
      with:
        name: sanitizer-${{ matrix.sanitizer }}-${{ matrix.os }}-results
        path: |
          build/Testing/Temporary/LastTest.log
          build/**/*.log



  # ============================================================================
  # Per-Project Flag + Backend Matrix Testing
  # Validates each library project under feature flag ON/OFF and backend variants
  # ============================================================================
  project-flag-backend-tests:
    name: "Project Matrix - ${{ matrix.project }} - ${{ matrix.external }} - ${{ matrix.backend_label }}"
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        include:
          - project: Core
            external: "ON"
            backend_label: default
            extra_cmake_flags: ""
          - project: Core
            external: "OFF"
            backend_label: default
            extra_cmake_flags: ""

          - project: Logging
            external: "ON"
            backend_label: LOGURU
            extra_cmake_flags: "-DLOGGING_BACKEND=LOGURU"
          - project: Logging
            external: "OFF"
            backend_label: NATIVE
            extra_cmake_flags: "-DLOGGING_BACKEND=NATIVE"
          - project: Logging
            external: "ON"
            backend_label: GLOG
            extra_cmake_flags: "-DLOGGING_BACKEND=GLOG"

          - project: Memory
            external: "ON"
            backend_label: default
            extra_cmake_flags: ""
          - project: Memory
            external: "OFF"
            backend_label: default
            extra_cmake_flags: ""

          - project: Parallel
            external: "ON"
            backend_label: std
            extra_cmake_flags: "-DPARALLEL_BACKEND=std"
          - project: Parallel
            external: "OFF"
            backend_label: openmp
            extra_cmake_flags: "-DPARALLEL_BACKEND=openmp"
          - project: Parallel
            external: "ON"
            backend_label: tbb
            extra_cmake_flags: "-DPARALLEL_BACKEND=tbb"

          - project: Profiler
            external: "OFF"
            backend_label: ITT
            extra_cmake_flags: "-DPROFILER_BACKEND=ITT"
          - project: Profiler
            external: "ON"
            backend_label: KINETO
            extra_cmake_flags: "-DPROFILER_BACKEND=KINETO"

          - project: Logging
            external: "ON"
            backend_label: SPDLOG
            extra_cmake_flags: "-DLOGGING_BACKEND=SPDLOG"

          - project: Vectorization
            external: "ON"
            backend_label: default
            extra_cmake_flags: ""
          - project: Vectorization
            external: "OFF"
            backend_label: default
            extra_cmake_flags: ""

          - project: Models
            external: "ON"
            backend_label: default
            extra_cmake_flags: ""
          - project: Models
            external: "OFF"
            backend_label: default
            extra_cmake_flags: ""

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        submodules: recursive

    - name: Cache dependencies
      uses: actions/cache@v4
      with:
        path: |
          ~/.cache
          build/ThirdParty
        key: ubuntu-project-matrix-${{ env.CACHE_VERSION }}-${{ hashFiles('**/CMakeLists.txt') }}
        restore-keys: |
          ubuntu-project-matrix-${{ env.CACHE_VERSION }}-

    - name: Install dependencies
      run: |
        chmod +x .github/workflows/install/install-deps-ubuntu.sh
        ./.github/workflows/install/install-deps-ubuntu.sh --with-tbb

    - name: Configure
      shell: bash
      run: |
        cmake -B build \
          -C .github/cmake/configure_cache.cmake \
          -S . \
          -G Ninja \
          -DCMAKE_BUILD_TYPE=Debug \
          -DCMAKE_C_COMPILER=clang \
          -DCMAKE_CXX_COMPILER=clang++ \
          -DCMAKE_CXX_STANDARD=17 \
          -DXSIGMA_ENABLE_EXTERNAL=${{ matrix.external }} \
          -DXSIGMA_LIBRARY_PROJECT=${{ matrix.project }} \
          -DBUILD_TESTING=ON \
          -DCORE_ENABLE_BENCHMARK=OFF \
          -DMEMORY_ENABLE_BENCHMARK=OFF \
          -DLOGGING_ENABLE_BENCHMARK=OFF \
          -DVECTORIZATION_ENABLE_BENCHMARK=OFF \
          -DPARALLEL_ENABLE_BENCHMARK=OFF \
          -DPROFILER_ENABLE_BENCHMARK=OFF \
          -DPARALLEL_ENABLE_TBB=ON \
          -DMEMORY_ENABLE_TBB=ON \
          -DMEMORY_GPU_BACKEND=none \
          ${{ matrix.extra_cmake_flags }}

    - name: Build
      run: cmake --build build -j 2

    - name: Run ${{ matrix.project }} tests
      shell: bash
      run: |
        regex=".*${{ matrix.project }}.*"
        if ctest --test-dir build -N -R "$regex" | grep -q "Total Tests: 0"; then
          echo "No project-specific test regex match found; running full test suite."
          ctest --test-dir build --output-on-failure -j 2
        else
          ctest --test-dir build --output-on-failure -R "$regex" -j 2
        fi

  # ============================================================================
  # Vectorization SIMD backend matrix (SSE, AVX-512, NEON, SVE)
  # x86 jobs: ubuntu-latest. AArch64 jobs: ubuntu-24.04-arm (requires public repo
  # or appropriate org plan for hosted ARM runners). AVX-512 is compiled whenever
  # the toolchain accepts -mavx512f; tests run only if the CPU has avx512f
  # (Scripts/helpers/cpu_isa.py). ubuntu-latest typically compiles but does not
  # execute AVX-512. CMake may still fall back to VECTORIZATION_CPU_BACKEND=no
  # if the compiler itself cannot emit the requested ISA.
  # ============================================================================
  vectorization-simd-backend-tests:
    name: "Vectorization ${{ matrix.vectorization_type }} — ${{ matrix.os }}"
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        include:
          - vectorization_type: no
            os: ubuntu-latest
          - vectorization_type: sse
            os: ubuntu-latest
          - vectorization_type: avx
            os: ubuntu-latest
          - vectorization_type: avx2
            os: ubuntu-latest
          - vectorization_type: avx512
            os: ubuntu-latest
          - vectorization_type: neon
            os: ubuntu-24.04-arm
          - vectorization_type: sve
            os: ubuntu-24.04-arm

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        submodules: recursive

    - name: Cache dependencies
      uses: actions/cache@v4
      with:
        path: |
          ~/.cache
          build/ThirdParty
        key: vec-simd-${{ matrix.vectorization_type }}-${{ env.CACHE_VERSION }}-${{ hashFiles('**/CMakeLists.txt', 'Library/Vectorization/**') }}
        restore-keys: |
          vec-simd-${{ matrix.vectorization_type }}-${{ env.CACHE_VERSION }}-

    - name: Install dependencies (Ubuntu)
      run: |
        chmod +x .github/workflows/install/install-deps-ubuntu.sh
        ./.github/workflows/install/install-deps-ubuntu.sh --with-tbb

    - name: Configure (Vectorization ${{ matrix.vectorization_type }})
      shell: bash
      run: |
        cmake -B build \
          -C .github/cmake/configure_cache.cmake \
          -S . \
          -G Ninja \
          -DCMAKE_BUILD_TYPE=Release \
          -DCMAKE_C_COMPILER=clang \
          -DCMAKE_CXX_COMPILER=clang++ \
          -DCMAKE_CXX_STANDARD=17 \
          -DVECTORIZATION_CPU_BACKEND=${{ matrix.vectorization_type }} \
          -DLOGGING_BACKEND=NATIVE \
          -DBUILD_TESTING=ON \
          -DCORE_ENABLE_BENCHMARK=OFF \
          -DMEMORY_ENABLE_BENCHMARK=OFF \
          -DLOGGING_ENABLE_BENCHMARK=OFF \
          -DVECTORIZATION_ENABLE_BENCHMARK=OFF \
          -DPARALLEL_ENABLE_BENCHMARK=OFF \
          -DPROFILER_ENABLE_BENCHMARK=OFF \
          -DPARALLEL_ENABLE_TBB=ON \
          -DMEMORY_ENABLE_TBB=ON \
          -DMEMORY_GPU_BACKEND=none

    - name: Build
      run: cmake --build build -j 2

    - name: Test (Vectorization focus)
      working-directory: ${{ github.workspace }}/build
      shell: bash
      run: |
        if [ "${{ matrix.vectorization_type }}" = "avx512" ]; then
          if ! python3 "${{ github.workspace }}/Scripts/helpers/cpu_isa.py" avx512; then
            echo "AVX-512 build succeeded; this CPU has no avx512f. Skipping test execution."
            exit 0
          fi
        fi
        regex="Vectorization"
        if ctest -N -R "$regex" | grep -q "Total Tests: 0"; then
          echo "No tests matched '$regex'; running full suite."
          ctest --output-on-failure -j 2
        else
          ctest --output-on-failure -R "$regex" -j 2
        fi

    - name: Upload Vectorization SIMD test logs
      if: failure()
      uses: actions/upload-artifact@v4
      with:
        name: vectorization-simd-${{ matrix.vectorization_type }}-logs
        path: |
          build/Testing/Temporary/LastTest.log
          build/**/*.log

  # ============================================================================
  # Optimization Flags Testing
  # Tests different compiler optimization levels to ensure code works correctly
  # across various optimization settings (-O0, -O2, -O3)
  # ============================================================================
  optimization-flags-test:
    name: Optimization - ${{ matrix.opt_level }}
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        opt_level: ["-O0", "-O2", "-O3"]
        include:
          - opt_level: "-O0"
            build_type: Debug
          - opt_level: "-O2"
            build_type: Release
          - opt_level: "-O3"
            build_type: Release

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        submodules: recursive

    - name: Cache dependencies
      uses: actions/cache@v4
      with:
        path: |
          ~/.cache
          build/ThirdParty
        key: ubuntu-opt-${{ env.CACHE_VERSION }}-${{ hashFiles('**/CMakeLists.txt') }}
        restore-keys: |
          ubuntu-opt-${{ env.CACHE_VERSION }}-

    - name: Install dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y \
          ninja-build \
          clang \
          cmake \
          python3 \
          libtbb-dev \
          valgrind \
          cppcheck \
          libnuma-dev \
          sccache

    - name: Configure with ${{ matrix.opt_level }}
      run: |
        cmake -B build \
          -C .github/cmake/configure_cache.cmake \
          -S . \
          -G Ninja \
          -DCMAKE_BUILD_TYPE=${{ matrix.build_type }} \
          -DCMAKE_C_COMPILER=clang \
          -DCMAKE_CXX_COMPILER=clang++ \
          -DCMAKE_C_FLAGS="${{ matrix.opt_level }}" \
          -DCMAKE_CXX_FLAGS="${{ matrix.opt_level }}" \
          -DLOGGING_BACKEND=LOGURU \
          -DBUILD_TESTING=ON

    - name: Build
      run: cmake --build build -j 2

    - name: Run Tests
      working-directory: ${{ github.workspace }}/build
      run: ctest --output-on-failure -j 2

  # ============================================================================
  # Link Time Optimization (LTO) Testing
  # Tests LTO enabled and disabled scenarios to ensure correct behavior
  # ============================================================================
  lto-tests:
    name: LTO - ${{ matrix.lto_enabled }}
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        lto_enabled: ["ON", "OFF"]

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        submodules: recursive

    - name: Cache dependencies
      uses: actions/cache@v4
      with:
        path: |
          ~/.cache
          build/ThirdParty
        key: ubuntu-lto-${{ env.CACHE_VERSION }}-${{ hashFiles('**/CMakeLists.txt') }}
        restore-keys: |
          ubuntu-lto-${{ env.CACHE_VERSION }}-

    - name: Install dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y \
          ninja-build \
          clang \
          cmake \
          python3 \
          libtbb-dev \
          cppcheck \
          libnuma-dev \
          sccache

    - name: Configure with LTO=${{ matrix.lto_enabled }}
      run: |
        cmake -B build \
          -C .github/cmake/configure_cache.cmake \
          -S . \
          -G Ninja \
          -DCMAKE_BUILD_TYPE=Release \
          -DCMAKE_C_COMPILER=clang \
          -DCMAKE_CXX_COMPILER=clang++ \
          -DLOGGING_BACKEND=LOGURU \
          -DBUILD_TESTING=ON \
          -DCORE_ENABLE_LTO=${{ matrix.lto_enabled }} \
          -DMEMORY_ENABLE_LTO=${{ matrix.lto_enabled }} \
          -DLOGGING_ENABLE_LTO=${{ matrix.lto_enabled }} \
          -DVECTORIZATION_ENABLE_LTO=${{ matrix.lto_enabled }} \
          -DPARALLEL_ENABLE_LTO=${{ matrix.lto_enabled }} \
          -DPROFILER_ENABLE_LTO=${{ matrix.lto_enabled }} \
          -DPARALLEL_ENABLE_TBB=ON \
          -DMEMORY_ENABLE_TBB=ON

    - name: Build
      run: cmake --build build -j 2

    - name: Run Tests
      working-directory: ${{ github.workspace }}/build
      run: ctest --output-on-failure -j 2

  # ============================================================================
  # Performance and Benchmark Tests
  # Runs performance benchmarks to detect regressions in computational performance
  # This job is non-blocking (failures don't fail the entire CI pipeline)
  # ============================================================================
  benchmark-tests:
    name: Performance Benchmarks
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        submodules: recursive

    - name: Cache dependencies
      uses: actions/cache@v4
      with:
        path: |
          ~/.cache
          build/ThirdParty
        key: ubuntu-bench-${{ env.CACHE_VERSION }}-${{ hashFiles('**/CMakeLists.txt') }}
        restore-keys: |
          ubuntu-bench-${{ env.CACHE_VERSION }}-

    - name: Install dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y \
          ninja-build \
          clang \
          cmake \
          python3 \
          libtbb-dev \
          valgrind \
          cppcheck \
          libnuma-dev \
          sccache

    - name: Configure for benchmarks
      run: |
        cmake -B build \
          -C .github/cmake/configure_cache.cmake \
          -S . \
          -G Ninja \
          -DCMAKE_BUILD_TYPE=Release \
          -DCMAKE_C_COMPILER=clang \
          -DCMAKE_CXX_COMPILER=clang++ \
          -DLOGGING_BACKEND=LOGURU \
          -DBUILD_TESTING=ON \
          -DCORE_ENABLE_BENCHMARK=ON \
          -DMEMORY_ENABLE_BENCHMARK=ON \
          -DLOGGING_ENABLE_BENCHMARK=ON \
          -DVECTORIZATION_ENABLE_BENCHMARK=ON \
          -DPARALLEL_ENABLE_BENCHMARK=ON \
          -DPROFILER_ENABLE_BENCHMARK=ON \
          -DCORE_ENABLE_LTO=ON \
          -DMEMORY_ENABLE_LTO=ON \
          -DLOGGING_ENABLE_LTO=ON \
          -DVECTORIZATION_ENABLE_LTO=ON \
          -DPARALLEL_ENABLE_LTO=ON \
          -DPROFILER_ENABLE_LTO=ON \
          -DPARALLEL_ENABLE_TBB=ON \
          -DMEMORY_ENABLE_TBB=ON

    - name: Build
      run: cmake --build build -j 2

    - name: Run Benchmarks
      working-directory: ${{ github.workspace }}/build
      run: |
        # Run benchmarks and save results
        ctest -R ".*[Bb]enchmark.*" --output-on-failure || true

    - name: Upload benchmark results
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: benchmark-results
        path: |
          build/Testing/Temporary/LastTest.log
          build/**/*benchmark*.log

  # ============================================================================
  # Sccache Performance Testing - Baseline (Without Sccache)
  # ============================================================================
  # Tests build performance without sccache for baseline comparison
  # Runs on Ubuntu, macOS, and Windows with consistent configuration
  # ============================================================================
  sccache-baseline-tests:
    name: "Sccache Baseline - ${{ matrix.os }} - ${{ matrix.build_type }}"
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        include:
          - name: "Ubuntu C++17 Release - Baseline (No Sccache)"
            os: ubuntu-latest
            build_type: Release
            cxx_std: 17
            logging_backend: LOGURU
            tbb_enabled: OFF
            cuda_enabled: OFF
            compiler_c: clang
            compiler_cxx: clang
            generator: Ninja
            cache_path: ~/.cache

          - name: "macOS C++17 Release - Baseline (No Sccache)"
            os: macos-latest
            build_type: Release
            cxx_std: 17
            logging_backend: LOGURU
            tbb_enabled: OFF
            cuda_enabled: OFF
            compiler_c: clang
            compiler_cxx: clang
            generator: Ninja
            cache_path: ~/Library/Caches

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        submodules: recursive
        fetch-depth: 0

    - name: Cache dependencies
      uses: actions/cache@v4
      with:
        path: |
          ${{ matrix.cache_path }}
          build/ThirdParty
        key: ${{ runner.os }}-sccache-baseline-${{ env.CACHE_VERSION }}-${{ hashFiles('**/CMakeLists.txt', 'ThirdParty/**') }}
        restore-keys: |
          ${{ runner.os }}-sccache-baseline-${{ env.CACHE_VERSION }}-

    # ===== Ubuntu Dependencies =====
    - name: Install dependencies (Ubuntu)
      if: runner.os == 'Linux'
      run: |
        chmod +x .github/workflows/install/install-deps-ubuntu.sh
        ./.github/workflows/install/install-deps-ubuntu.sh --with-tbb

    # ===== macOS Dependencies =====
    - name: Install dependencies (macOS)
      if: runner.os == 'macOS'
      run: |
        chmod +x .github/workflows/install/install-deps-macos.sh
        ./.github/workflows/install/install-deps-macos.sh --with-tbb

    # ===== CMake Configuration (Baseline - No Sccache) =====
    - name: Configure CMake (Ubuntu/macOS - Baseline)
      if: runner.os != 'Windows'
      run: |
        cmake -B build \
          -C .github/cmake/configure_cache.cmake \
          -S . \
          -G Ninja \
          -DCMAKE_BUILD_TYPE=Release \
          -DCMAKE_C_COMPILER=clang \
          -DCMAKE_CXX_COMPILER=clang++ \
          -DLOGGING_BACKEND=LOGURU \
          -DBUILD_TESTING=ON \
          -DCORE_ENABLE_BENCHMARK=OFF \
          -DMEMORY_ENABLE_BENCHMARK=OFF \
          -DLOGGING_ENABLE_BENCHMARK=OFF \
          -DVECTORIZATION_ENABLE_BENCHMARK=OFF \
          -DPARALLEL_ENABLE_BENCHMARK=OFF \
          -DPROFILER_ENABLE_BENCHMARK=OFF

    # ===== Build and Test (Baseline - No Sccache) =====
    - name: Record build start time (Baseline)
      shell: bash
      run: |
        echo "BUILD_START_TIME=$(date +%s)" >> $GITHUB_ENV

    - name: Build (Baseline - No Sccache)
      run: cmake --build build --config ${{ matrix.build_type }} -j 2

    - name: Record build end time (Baseline)
      shell: bash
      run: |
        echo "BUILD_END_TIME=$(date +%s)" >> $GITHUB_ENV
        BUILD_DURATION=$((BUILD_END_TIME - BUILD_START_TIME))
        echo "BUILD_DURATION_BASELINE=$BUILD_DURATION" >> $GITHUB_ENV

    - name: Run Tests (Baseline)
      working-directory: ${{ github.workspace }}/build
      run: ctest --output-on-failure --build-config ${{ matrix.build_type }} -j 2

    - name: Report Baseline Build Metrics
      shell: bash
      run: |
        echo "=== Sccache Baseline Build Metrics ==="
        echo "Platform: ${{ runner.os }}"
        echo "Build Type: ${{ matrix.build_type }}"
        echo "C++ Standard: ${{ matrix.cxx_std }}"
        echo "Sccache Enabled: NO (Baseline)"
        echo "Build Duration: ${{ env.BUILD_DURATION_BASELINE }} seconds"
        echo ""

  # ============================================================================
  # Sccache Performance Testing - With Sccache Enabled
  # ============================================================================
  # Tests build performance with sccache enabled for comparison
  # Uses CMAKE_CXX_COMPILER_LAUNCHER to enable sccache
  # ============================================================================
  sccache-enabled-tests:
    name: "Sccache Enabled - ${{ matrix.os }} - ${{ matrix.build_type }}"
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        include:
          - name: "Ubuntu C++17 Release - Sccache Enabled"
            os: ubuntu-latest
            build_type: Release
            cxx_std: 17
            logging_backend: LOGURU
            tbb_enabled: ON
            cuda_enabled: OFF
            compiler_c: clang
            compiler_cxx: clang
            generator: Ninja
            cache_path: ~/.cache

          - name: "macOS C++17 Release - Sccache Enabled"
            os: macos-latest
            build_type: Release
            cxx_std: 17
            logging_backend: LOGURU
            tbb_enabled: ON
            cuda_enabled: OFF
            compiler_c: clang
            compiler_cxx: clang
            generator: Ninja
            cache_path: ~/Library/Caches

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        submodules: recursive
        fetch-depth: 0

    - name: Cache dependencies
      uses: actions/cache@v4
      with:
        path: |
          ${{ matrix.cache_path }}
          build/ThirdParty
        key: ${{ runner.os }}-sccache-enabled-${{ env.CACHE_VERSION }}-${{ hashFiles('**/CMakeLists.txt', 'ThirdParty/**') }}
        restore-keys: |
          ${{ runner.os }}-sccache-enabled-${{ env.CACHE_VERSION }}-

    - name: Cache sccache directory
      uses: actions/cache@v4
      with:
        path: |
          ~/.cache/sccache
          ~/Library/Caches/sccache
        key: ${{ runner.os }}-sccache-${{ env.CACHE_VERSION }}-${{ github.sha }}
        restore-keys: |
          ${{ runner.os }}-sccache-${{ env.CACHE_VERSION }}-

    # ===== Ubuntu Dependencies =====
    - name: Install dependencies (Ubuntu)
      if: runner.os == 'Linux'
      run: |
        chmod +x .github/workflows/install/install-deps-ubuntu.sh
        ./.github/workflows/install/install-deps-ubuntu.sh --with-tbb

    # ===== macOS Dependencies =====
    - name: Install dependencies (macOS)
      if: runner.os == 'macOS'
      run: |
        chmod +x .github/workflows/install/install-deps-macos.sh
        ./.github/workflows/install/install-deps-macos.sh --with-tbb

    # ===== Windows Dependencies =====
    - name: Install dependencies (Windows)
      if: runner.os == 'Windows'
      shell: pwsh
      run: |
        & .\.github\workflows\install\install-deps-windows.ps1

    # ===== Install Sccache =====
    - name: Install sccache (Ubuntu)
      if: runner.os == 'Linux'
      run: |
        echo "=== Installing sccache on Ubuntu ==="
        mkdir -p ~/.local/bin
        curl -L https://github.com/mozilla/sccache/releases/download/v${{ env.SCCACHE_VERSION }}/sccache-v${{ env.SCCACHE_VERSION }}-x86_64-unknown-linux-musl.tar.gz \
          | tar xz -C ~/.local/bin --strip-components=1
        chmod +x ~/.local/bin/sccache
        echo "$HOME/.local/bin" >> $GITHUB_PATH
        sccache --version

    - name: Install sccache (macOS)
      if: runner.os == 'macOS'
      run: |
        echo "=== Installing sccache on macOS ==="
        mkdir -p ~/.local/bin
        curl -L https://github.com/mozilla/sccache/releases/download/v${{ env.SCCACHE_VERSION }}/sccache-v${{ env.SCCACHE_VERSION }}-x86_64-apple-darwin.tar.gz \
          | tar xz -C ~/.local/bin --strip-components=1
        chmod +x ~/.local/bin/sccache
        echo "$HOME/.local/bin" >> $GITHUB_PATH
        sccache --version

    # ===== CMake Configuration (With Sccache) =====
    - name: Configure CMake (Ubuntu/macOS - With Sccache)
      if: runner.os != 'Windows'
      run: |
        cmake -B build \
          -C .github/cmake/configure_cache.cmake \
          -S . \
          -G Ninja \
          -DCMAKE_CXX_COMPILER_LAUNCHER=sccache \
          -DCMAKE_BUILD_TYPE=Release \
          -DCMAKE_C_COMPILER=clang \
          -DCMAKE_CXX_COMPILER=clang++ \
          -DCORE_ENABLE_BENCHMARK=OFF \
          -DMEMORY_ENABLE_BENCHMARK=OFF \
          -DLOGGING_ENABLE_BENCHMARK=OFF \
          -DVECTORIZATION_ENABLE_BENCHMARK=OFF \
          -DPARALLEL_ENABLE_BENCHMARK=OFF \
          -DPROFILER_ENABLE_BENCHMARK=OFF \
          -DBUILD_TESTING=ON

    - name: Configure CMake (Windows - With Sccache)
      if: runner.os == 'Windows'
      shell: pwsh
      run: |
        cmake -B build `
          -C .github/cmake/configure_cache.cmake `
          -S . `
          -G "${{ matrix.generator }}" `
          -DCMAKE_BUILD_TYPE=${{ matrix.build_type }} `
          -DCMAKE_C_COMPILER=${{ matrix.compiler_c }} `
          -DCMAKE_CXX_COMPILER=${{ matrix.compiler_cxx }} `
          -DCMAKE_CXX_COMPILER_LAUNCHER=sccache `
          -DCMAKE_CXX_STANDARD=${{ matrix.cxx_std }} `
          -DLOGGING_BACKEND=${{ matrix.logging_backend }} `
          -DBUILD_TESTING=ON `
          -DCORE_ENABLE_BENCHMARK=OFF `
          -DMEMORY_ENABLE_BENCHMARK=OFF `
          -DLOGGING_ENABLE_BENCHMARK=OFF `
          -DVECTORIZATION_ENABLE_BENCHMARK=OFF `
          -DPARALLEL_ENABLE_BENCHMARK=OFF `
          -DPROFILER_ENABLE_BENCHMARK=OFF `
          -DMEMORY_GPU_BACKEND=${{ matrix.cuda_enabled == 'ON' && 'cuda' || 'none' }}

    # ===== Build and Test (With Sccache) =====
    - name: Record build start time (Sccache)
      shell: bash
      run: |
        echo "BUILD_START_TIME=$(date +%s)" >> $GITHUB_ENV

    - name: Build (With Sccache)
      run: cmake --build build --config ${{ matrix.build_type }} -j 2

    - name: Record build end time (Sccache)
      shell: bash
      run: |
        echo "BUILD_END_TIME=$(date +%s)" >> $GITHUB_ENV
        BUILD_DURATION=$((BUILD_END_TIME - BUILD_START_TIME))
        echo "BUILD_DURATION_SCCACHE=$BUILD_DURATION" >> $GITHUB_ENV

    - name: Run Tests (With Sccache)
      working-directory: ${{ github.workspace }}/build
      run: ctest --output-on-failure --build-config ${{ matrix.build_type }} -j 2

    - name: Report Sccache Build Metrics
      shell: bash
      run: |
        echo "=== Sccache Enabled Build Metrics ==="
        echo "Platform: ${{ runner.os }}"
        echo "Build Type: ${{ matrix.build_type }}"
        echo "C++ Standard: ${{ matrix.cxx_std }}"
        echo "Sccache Enabled: YES"
        echo "Build Duration: ${{ env.BUILD_DURATION_SCCACHE }} seconds"
        echo ""
        sccache --show-stats || echo "Sccache stats unavailable"

  # ============================================================================
  # Code Coverage Testing - Clang
  # ============================================================================
  clang-coverage-tests:
    name: Clang Coverage Tests
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: "Ubuntu Clang Coverage - C++17"
            os: ubuntu-latest
            build_type: Release
            cxx_std: 17
            logging_backend: NATIVE
            tbb_enabled: OFF
            cuda_enabled: OFF
            compiler_c: clang
            compiler_cxx: clang
            generator: Ninja
            cache_path: ~/.cache

    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        submodules: recursive
        fetch-depth: 0

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'

    - name: Install dependencies
      shell: bash
      run: |
        bash .github/workflows/install/install-deps-ubuntu.sh --with-tbb=OFF --with-cuda=OFF

    - name: Install Clang
      shell: bash
      run: |
        sudo apt-get update
        sudo apt-get install -y clang llvm

    - name: Verify Clang installation
      shell: bash
      run: |
        clang --version
        llvm-cov --version

    - name: Install coverage tool package
      shell: bash
      run: |
        pip install -e "Tools/coverage[test]"

    - name: Run coverage tool test suite
      shell: bash
      run: |
        python -m pytest Tools/coverage/tests/ -v

    - name: Run coverage build
      shell: bash
      run: |
        # Configure CMake with coverage enabled
        cmake -B build \
          -S . \
          -G Ninja \
          -DCMAKE_BUILD_TYPE=Debug \
          -DCMAKE_C_COMPILER=clang \
          -DCMAKE_CXX_COMPILER=clang++ \
          -DCMAKE_CXX_STANDARD=17 \
          -DLOGGING_BACKEND=NATIVE \
          -DBUILD_TESTING=ON \
          -DCORE_ENABLE_COVERAGE=ON \
          -DMEMORY_ENABLE_COVERAGE=ON \
          -DLOGGING_ENABLE_COVERAGE=ON \
          -DVECTORIZATION_ENABLE_COVERAGE=ON \
          -DPARALLEL_ENABLE_COVERAGE=ON \
          -DPROFILER_ENABLE_COVERAGE=ON \
          -DMEMORY_GPU_BACKEND=none

        # Build
        cmake --build build -j 2

        # Run tests
        ctest --test-dir build --output-on-failure -j 2

        # Run coverage analysis
        coverage-tool --build=build

    - name: Verify JSON report generation
      shell: bash
      run: |
        if [ ! -f "build/coverage_report/coverage_summary.json" ]; then
          echo "Error: JSON coverage report not generated"
          exit 1
        fi
        echo "✓ JSON coverage report generated successfully"

    - name: Verify HTML report generation
      shell: bash
      run: |
        if [ ! -f "build/coverage_report/html/index.html" ]; then
          echo "Error: HTML coverage report not generated"
          exit 1
        fi
        echo "✓ HTML coverage report generated successfully"

    - name: Display coverage metrics
      shell: bash
      run: |
        python -c "
        import json
        with open('build/coverage_report/coverage_summary.json') as f:
            data = json.load(f)
            line_cov = data['summary']['line_coverage']
            func_cov = data['summary']['function_coverage']
            print('=== Coverage Metrics ===')
            print(f'Line Coverage: {line_cov[\"covered\"]}/{line_cov[\"total\"]} ({line_cov[\"percent\"]}%)')
            print(f'Function Coverage: {func_cov[\"covered\"]}/{func_cov[\"total\"]} ({func_cov[\"percent\"]}%)')
            print(f'Files Analyzed: {len(data[\"files\"])}')
        "

    - name: Generate LCOV report for Codecov
      shell: bash
      run: |
        # Convert JSON coverage to LCOV format for Codecov
        if [ -f "build/coverage_report/coverage.lcov" ]; then
          echo "✓ LCOV report already exists"
        else
          echo "Note: LCOV report will be generated by coverage tools"
        fi

    - name: Upload coverage to Codecov
      uses: codecov/codecov-action@v4
      with:
        files: ./build/coverage_report/coverage.lcov
        flags: clang-coverage
        name: clang-coverage-report
        fail_ci_if_error: false
        verbose: true
      env:
        CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}

    - name: Upload coverage artifacts
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: clang-coverage-reports
        path: build/coverage_report/
        retention-days: 30

  # ============================================================================
  # Windows build CLI smoke test (no compile — verifies Python entrypoints)
  # ============================================================================
  windows-cli-smoke:
    name: Windows — build CLI smoke
    runs-on: windows-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.x'

    - name: Install Python dependencies
      run: |
        python -m pip install --upgrade pip
        pip install colorama==0.4.6

    - name: Smoke-test Scripts/setup.py --help
      shell: pwsh
      run: |
        python Scripts\setup.py --help
        if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

    - name: Smoke-test Scripts/setup_bazel.py --help
      shell: pwsh
      run: |
        python Scripts\setup_bazel.py --help
        if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

  # ============================================================================
  # Bazel Build Matrix — default config
  # Real `bazel build`/`bazel test` runs via Scripts/setup_bazel.py, mirroring the
  # CMake build-matrix job above but through the Bazel build system. Windows is not
  # included here yet (only smoke-tested above via --help) — the Bazel setup has not
  # been verified end-to-end on Windows.
  # ============================================================================
  bazel-build-matrix:
    name: "Bazel - ${{ matrix.os }}"
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        submodules: recursive
        fetch-depth: 0

    - name: Setup Bazel
      uses: bazel-contrib/setup-bazel@0.8.1
      with:
        bazelisk-cache: true
        disk-cache: bazel-${{ runner.os }}-default-${{ env.CACHE_VERSION }}
        repository-cache: true

    - name: Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.x'

    - name: Install Python dependencies
      run: |
        python3 -m pip install --upgrade pip
        pip install colorama==0.4.6

    - name: Bazel build + test (default config)
      working-directory: Scripts
      run: python3 setup_bazel.py config.build.test

  # ============================================================================
  # Bazel TBB Tests
  # Exercises --config=tbb specifically: the Bazel-side MEMORY_HAS_TBB define was
  # historically not paired with an actual @tbb dep (see Docs/BAZEL_USER_GUIDE.md's
  # Known Gaps section) — this job is the regression guard for that class of bug.
  # ============================================================================
  bazel-tbb-tests:
    name: "Bazel TBB - ${{ matrix.os }}"
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        submodules: recursive
        fetch-depth: 0

    - name: Setup Bazel
      uses: bazel-contrib/setup-bazel@0.8.1
      with:
        bazelisk-cache: true
        disk-cache: bazel-${{ runner.os }}-tbb-${{ env.CACHE_VERSION }}
        repository-cache: true

    - name: Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.x'

    - name: Install Python dependencies
      run: |
        python3 -m pip install --upgrade pip
        pip install colorama==0.4.6

    - name: Bazel build + test (TBB)
      working-directory: Scripts
      run: python3 setup_bazel.py config.build.test.tbb

  # ============================================================================
  # Bazel Project + Backend Matrix
  # Mirrors the CMake project-flag-backend-tests job's project/backend coverage via
  # Scripts/setup_bazel.py --project.NAME. XSIGMA_ENABLE_EXTERNAL has no Bazel
  # equivalent (setup_bazel.py warns and no-ops on the "external" token -- see
  # Docs/BAZEL_USER_GUIDE.md), so each project/backend combination appears once
  # here instead of twice.
  # ============================================================================
  bazel-project-flag-backend-tests:
    name: "Bazel Project Matrix - ${{ matrix.project }} - ${{ matrix.backend_label }}"
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        include:
          - project: core
            backend_label: default
            extra_config: ""
          - project: memory
            backend_label: default
            extra_config: ""
          - project: logging
            backend_label: LOGURU
            extra_config: logging_loguru
          - project: logging
            backend_label: NATIVE
            extra_config: logging_native
          - project: logging
            backend_label: GLOG
            extra_config: logging_glog
          - project: parallel
            backend_label: std
            extra_config: parallel.std
          - project: parallel
            backend_label: openmp
            extra_config: parallel.openmp
          - project: parallel
            backend_label: tbb
            extra_config: parallel.tbb
          - project: profiler
            backend_label: ITT
            extra_config: profiler_itt
          - project: profiler
            backend_label: KINETO
            extra_config: profiler_kineto
          - project: logging
            backend_label: SPDLOG
            extra_config: logging_spdlog
          - project: vectorization
            backend_label: default
            extra_config: ""
          - project: models
            backend_label: default
            extra_config: ""

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        submodules: recursive
        fetch-depth: 0

    - name: Setup Bazel
      uses: bazel-contrib/setup-bazel@0.8.1
      with:
        bazelisk-cache: true
        disk-cache: bazel-project-${{ matrix.project }}-${{ matrix.backend_label }}-${{ env.CACHE_VERSION }}
        repository-cache: true

    - name: Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.x'

    - name: Install Python dependencies
      run: |
        python3 -m pip install --upgrade pip
        pip install colorama==0.4.6

    - name: Install OpenMP (Bazel openmp backend)
      if: matrix.backend_label == 'openmp'
      run: sudo apt-get update && sudo apt-get install -y libomp-dev

    - name: Bazel build + test (${{ matrix.project }} / ${{ matrix.backend_label }})
      working-directory: Scripts
      shell: bash
      run: |
        if [ -n "${{ matrix.extra_config }}" ]; then
          python3 setup_bazel.py config.build.test.${{ matrix.extra_config }} --project.${{ matrix.project }}
        else
          python3 setup_bazel.py config.build.test --project.${{ matrix.project }}
        fi

  # ============================================================================
  # Bazel Vectorization SIMD Backend Matrix
  # Mirrors the CMake vectorization-simd-backend-tests job. This is the direct
  # regression guard for the ARM-host select() ambiguity bug found in this project's
  # Bazel/CMake alignment work: on aarch64, an explicit --define=vectorization_type=X
  # for any tier except neon used to fail analysis entirely (see Docs/BAZEL_USER_GUIDE.md).
  # ============================================================================
  bazel-vectorization-simd-tests:
    name: "Bazel Vectorization ${{ matrix.vectorization_type }} — ${{ matrix.os }}"
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        include:
          - vectorization_type: no
            os: ubuntu-latest
          - vectorization_type: sse
            os: ubuntu-latest
          - vectorization_type: avx
            os: ubuntu-latest
          - vectorization_type: avx2
            os: ubuntu-latest
          - vectorization_type: avx512
            os: ubuntu-latest
          - vectorization_type: neon
            os: ubuntu-24.04-arm
          - vectorization_type: sve
            os: ubuntu-24.04-arm

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        submodules: recursive
        fetch-depth: 0

    - name: Setup Bazel
      uses: bazel-contrib/setup-bazel@0.8.1
      with:
        bazelisk-cache: true
        disk-cache: bazel-vec-simd-${{ matrix.vectorization_type }}-${{ env.CACHE_VERSION }}
        repository-cache: true

    - name: Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.x'

    - name: Install Python dependencies
      run: |
        python3 -m pip install --upgrade pip
        pip install colorama==0.4.6

    # AVX-512 still compiles on ubuntu-latest. Hosted runners often list
    # avx512f in cpuinfo but SIGILL on the instructions; skip the test run
    # unless Scripts/helpers/cpu_isa.py reports avx512f (same as CMake).
    - name: Bazel build + test (Vectorization ${{ matrix.vectorization_type }})
      working-directory: Scripts
      run: |
        if [ "${{ matrix.vectorization_type }}" = "avx512" ]; then
          if ! python3 "${{ github.workspace }}/Scripts/helpers/cpu_isa.py" avx512; then
            echo "AVX-512 compile-only: this CPU has no usable avx512f."
            python3 setup_bazel.py config.build.avx512 --project.vectorization
            exit 0
          fi
        fi
        python3 setup_bazel.py config.build.test.${{ matrix.vectorization_type }} --project.vectorization

  # ============================================================================
  # Bazel Metal Tests — macOS only
  # Real Metal GPU dispatch through both Library/Memory and Library/Vectorization's
  # independent GPU backends. Runs on real Metal hardware (macos-latest runners have
  # a GPU), unlike every other job here which only exercises CPU paths.
  # ============================================================================
  bazel-metal-tests:
    name: "Bazel Metal - macOS"
    runs-on: macos-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        submodules: recursive
        fetch-depth: 0

    - name: Setup Bazel
      uses: bazel-contrib/setup-bazel@0.8.1
      with:
        bazelisk-cache: true
        disk-cache: bazel-macos-metal-${{ env.CACHE_VERSION }}
        repository-cache: true

    - name: Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.x'

    - name: Install Python dependencies
      run: |
        python3 -m pip install --upgrade pip
        pip install colorama==0.4.6

    - name: Bazel build + test (Metal)
      working-directory: Scripts
      run: python3 setup_bazel.py config.build.test.metal

  # ============================================================================
  # CMake GPU backends — CUDA / HIP / Metal
  # Hosted runners have no NVIDIA/AMD GPU. CUDA (Linux) and HIP jobs compile
  # the GPU path and run the CPU test suite; device tests GTEST_SKIP when
  # cudaGetDeviceCount is 0. CUDA (Windows) is build-only (see matrix comment
  # below — no driver-less nvcuda.dll stub exists for Windows). Metal runs on
  # real macOS GPU hardware.
  # ============================================================================
  cmake-gpu-backend-tests:
    name: "CMake GPU - ${{ matrix.backend }} — ${{ matrix.os }}"
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        include:
          - backend: cuda
            os: ubuntu-latest
            extra_cmake: "-DMEMORY_GPU_BACKEND=cuda -DVECTORIZATION_GPU_BACKEND=cuda"
          # Windows CUDA is build-only: NVIDIA ships a driver stub (libcuda.so)
          # for Linux CI but not an equivalent nvcuda.dll for Windows, so a
          # CUDA-linked .exe compiles/links fine here but cannot load on a
          # driver-less runner. This still catches real compile/link
          # regressions (e.g. windows.h macro pollution via NVTX headers,
          # missing CUPTI runtime DLL staging) without a flaky test step.
          # Build Vectorization benchmarks too, so Clang CUDA benchmark
          # compiler options are checked even though execution is skipped.
          - backend: cuda
            os: windows-latest
            extra_cmake: "-DMEMORY_GPU_BACKEND=cuda -DVECTORIZATION_GPU_BACKEND=cuda"
          - backend: hip
            os: ubuntu-latest
            extra_cmake: "-DMEMORY_GPU_BACKEND=hip -DVECTORIZATION_GPU_BACKEND=hip -DCMAKE_HIP_ARCHITECTURES=gfx90a -DPROJECT_HIP_ARCH_OPTIONS=gfx90a"
          - backend: metal
            os: macos-latest
            extra_cmake: "-DMEMORY_GPU_BACKEND=metal -DVECTORIZATION_GPU_BACKEND=metal"

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        submodules: recursive

    - name: Install dependencies (Ubuntu)
      if: runner.os == 'Linux'
      run: |
        chmod +x .github/workflows/install/install-deps-ubuntu.sh
        extra=""
        if [ "${{ matrix.backend }}" = "cuda" ]; then extra="--with-cuda"; fi
        if [ "${{ matrix.backend }}" = "hip" ]; then extra="--with-hip"; fi
        ./.github/workflows/install/install-deps-ubuntu.sh --with-tbb $extra

    - name: Select GCC 13 headers for Clang CUDA
      if: runner.os == 'Linux' && matrix.backend == 'cuda'
      shell: bash
      run: |
        # Clang 18 selects the newest installed GCC by default. GCC 14's
        # <limits> exposes __float128, which is unsupported on CUDA targets.
        # Keep host C++ and CUDA compilation on the same compatible headers.
        sudo apt-get install -y g++-13
        gcc_install_dir="$(dirname "$(g++-13 -print-libgcc-file-name)")"
        echo "CXXFLAGS=${CXXFLAGS:+$CXXFLAGS }--gcc-install-dir=$gcc_install_dir" >> "$GITHUB_ENV"
        echo "CUDAFLAGS=${CUDAFLAGS:+$CUDAFLAGS }--gcc-install-dir=$gcc_install_dir" >> "$GITHUB_ENV"

    - name: Install dependencies (macOS)
      if: runner.os == 'macOS'
      run: |
        chmod +x .github/workflows/install/install-deps-macos.sh
        ./.github/workflows/install/install-deps-macos.sh --with-tbb

    - name: Install dependencies (Windows)
      if: runner.os == 'Windows'
      shell: pwsh
      run: |
        & .\.github\workflows\install\install-deps-windows.ps1 -WithCuda

    - name: Setup macOS linker environment
      if: runner.os == 'macOS'
      run: |
        echo "LDFLAGS=-L/opt/homebrew/opt/llvm/lib/c++ -L/opt/homebrew/opt/llvm/lib -Wl,-rpath,/opt/homebrew/opt/llvm/lib/c++ -Wl,-rpath,/opt/homebrew/opt/llvm/lib" >> $GITHUB_ENV
        echo "CPPFLAGS=-I/opt/homebrew/opt/llvm/include" >> $GITHUB_ENV
        echo "/opt/homebrew/opt/llvm/bin" >> $GITHUB_PATH

    - name: Configure (CMake ${{ matrix.backend }})
      shell: bash
      run: |
        # TBB is tested separately on Windows (see build-matrix); this job's
        # Windows install step doesn't provision it.
        tbb_flags="-DPARALLEL_ENABLE_TBB=ON -DMEMORY_ENABLE_TBB=ON"
        if [ "${{ runner.os }}" = "Windows" ]; then
          tbb_flags="-DPARALLEL_ENABLE_TBB=OFF -DMEMORY_ENABLE_TBB=OFF"
        fi
        cmake -B build \
          -C .github/cmake/configure_cache.cmake \
          -S . \
          -G Ninja \
          -DCMAKE_BUILD_TYPE=Release \
          -DCMAKE_C_COMPILER=clang \
          -DCMAKE_CXX_COMPILER=clang++ \
          -DCMAKE_CXX_STANDARD=20 \
          -DLOGGING_BACKEND=NATIVE \
          -DBUILD_TESTING=ON \
          -DCORE_ENABLE_BENCHMARK=OFF \
          -DMEMORY_ENABLE_BENCHMARK=OFF \
          -DLOGGING_ENABLE_BENCHMARK=OFF \
          -DVECTORIZATION_ENABLE_BENCHMARK=${{ runner.os == 'Windows' && matrix.backend == 'cuda' && 'ON' || 'OFF' }} \
          -DPARALLEL_ENABLE_BENCHMARK=OFF \
          -DPROFILER_ENABLE_BENCHMARK=OFF \
          $tbb_flags \
          ${{ matrix.extra_cmake }}

    - name: Build
      run: cmake --build build -j 2

    - name: Test
      if: runner.os != 'Windows'
      working-directory: ${{ github.workspace }}/build
      run: |
        # Toolkit install has no NVIDIA driver; DT_NEEDED libcuda.so.1 must
        # resolve to the CUDA stub (often under targets/.../lib/stubs, and
        # often named only libcuda.so until a .so.1 symlink is created).
        stubs=""
        for d in \
            /usr/local/cuda/lib64/stubs \
            /usr/local/cuda/targets/x86_64-linux/lib/stubs \
            /usr/local/cuda-12.6/lib64/stubs \
            /usr/local/cuda-12.6/targets/x86_64-linux/lib/stubs; do
          if [ -e "${d}/libcuda.so" ] || [ -e "${d}/libcuda.so.1" ]; then
            stubs="${d}"
            break
          fi
        done
        if [ -n "${stubs}" ]; then
          if [ -e "${stubs}/libcuda.so" ] && [ ! -e "${stubs}/libcuda.so.1" ]; then
            ln -sf libcuda.so "${stubs}/libcuda.so.1" 2>/dev/null \
              || sudo ln -sf libcuda.so "${stubs}/libcuda.so.1"
          fi
          export LD_LIBRARY_PATH="${stubs}:${LD_LIBRARY_PATH:-}"
        fi
        rocm_lib="${ROCM_PATH:-${HIP_PATH:-/opt/rocm}}/lib"
        if [ -d "${rocm_lib}" ]; then
          export LD_LIBRARY_PATH="${rocm_lib}:${LD_LIBRARY_PATH:-}"
        fi
        if [ -f "${{ github.workspace }}/ThirdParty/svml/unix/lib/libintlc.so.5" ]; then
          export LD_LIBRARY_PATH="${{ github.workspace }}/ThirdParty/svml/unix/lib:${LD_LIBRARY_PATH:-}"
        fi
        ctest --output-on-failure -j 2

    - name: Upload GPU CMake logs
      if: failure()
      uses: actions/upload-artifact@v4
      with:
        name: cmake-gpu-${{ matrix.backend }}-logs
        path: |
          build/Testing/Temporary/LastTest.log
          build/**/*.log

  # ============================================================================
  # Bazel CUDA / HIP (compile + test; device tests skip without a GPU)
  # ============================================================================
  bazel-cuda-tests:
    name: "Bazel CUDA - Ubuntu"
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        submodules: recursive
        fetch-depth: 0

    - name: Install CUDA toolkit
      run: |
        chmod +x .github/workflows/install/install-cuda-ubuntu.sh
        .github/workflows/install/install-cuda-ubuntu.sh

    - name: Setup Bazel
      uses: bazel-contrib/setup-bazel@0.8.1
      with:
        bazelisk-cache: true
        disk-cache: bazel-ubuntu-cuda-${{ env.CACHE_VERSION }}
        repository-cache: true

    - name: Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.x'

    - name: Install Python dependencies
      run: |
        python3 -m pip install --upgrade pip
        pip install colorama==0.4.6

    - name: Bazel build + test (CUDA)
      working-directory: Scripts
      env:
        LD_LIBRARY_PATH: /usr/local/cuda/lib64/stubs:/usr/local/cuda/targets/x86_64-linux/lib/stubs:${{ env.LD_LIBRARY_PATH }}
      run: python3 setup_bazel.py config.build.test.cuda

  bazel-hip-tests:
    name: "Bazel HIP - Ubuntu"
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        submodules: recursive
        fetch-depth: 0

    - name: Install HIP toolkit
      run: |
        chmod +x .github/workflows/install/install-hip-ubuntu.sh
        .github/workflows/install/install-hip-ubuntu.sh

    - name: Setup Bazel
      uses: bazel-contrib/setup-bazel@0.8.1
      with:
        bazelisk-cache: true
        disk-cache: bazel-ubuntu-hip-${{ env.CACHE_VERSION }}
        repository-cache: true

    - name: Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.x'

    - name: Install Python dependencies
      run: |
        python3 -m pip install --upgrade pip
        pip install colorama==0.4.6

    - name: Bazel build + test (HIP)
      working-directory: Scripts
      run: python3 setup_bazel.py config.build.test.hip

  # ============================================================================
  # CMake Metal is covered by cmake-gpu-backend-tests (macos). This job is the
  # CMake counterpart to bazel-metal-tests, scoped to Memory+Vectorization
  # packet-size / SLEEF / Accelerate / shared / NUMA / memkind / mimalloc-off.
  # ============================================================================
  feature-flag-tests:
    name: "Feature - ${{ matrix.name }}"
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        include:
          - name: "NUMA"
            os: ubuntu-latest
            extra_cmake: "-DMEMORY_ENABLE_NUMA=ON"
            extra_deps: ""
          - name: "memkind"
            os: ubuntu-latest
            extra_cmake: "-DMEMORY_ENABLE_MEMKIND=ON"
            extra_deps: "--with-memkind"
          - name: "mimalloc-OFF"
            os: ubuntu-latest
            extra_cmake: "-DMEMORY_ENABLE_MIMALLOC=OFF"
            extra_deps: ""
          - name: "mimalloc-stats"
            os: ubuntu-latest
            extra_cmake: "-DMEMORY_ENABLE_MIMALLOC=ON -DMEMORY_ENABLE_MIMALLOC_STATS=ON"
            extra_deps: ""
          - name: "shared-libs"
            os: ubuntu-latest
            extra_cmake: "-DBUILD_SHARED_LIBS=ON"
            extra_deps: ""
          - name: "magic_enum-OFF"
            os: ubuntu-latest
            extra_cmake: "-DCORE_ENABLE_MAGICENUM=OFF"
            extra_deps: ""
          - name: "examples"
            os: ubuntu-latest
            extra_cmake: "-DCORE_ENABLE_EXAMPLES=ON -DMEMORY_ENABLE_EXAMPLES=ON -DLOGGING_ENABLE_EXAMPLES=ON -DVECTORIZATION_ENABLE_EXAMPLES=ON -DPARALLEL_ENABLE_EXAMPLES=ON -DPROFILER_ENABLE_EXAMPLES=ON -DMODELS_ENABLE_EXAMPLES=ON"
            extra_deps: ""
          - name: "packet-size-8"
            os: ubuntu-latest
            extra_cmake: "-DVECTORIZATION_PACKET_SIZE=8 -DVECTORIZATION_CPU_BACKEND=avx2"
            extra_deps: ""
          - name: "sleef"
            os: ubuntu-24.04-arm
            extra_cmake: "-DVECTORIZATION_ENABLE_SLEEF=ON -DVECTORIZATION_CPU_BACKEND=neon"
            extra_deps: ""
          - name: "accelerate"
            os: macos-latest
            extra_cmake: "-DVECTORIZATION_ENABLE_ACCELERATE=ON -DVECTORIZATION_CPU_BACKEND=neon"
            extra_deps: ""
          - name: "lto-thin"
            os: ubuntu-latest
            extra_cmake: "-DCORE_LTO_MODE=thin -DMEMORY_LTO_MODE=thin -DLOGGING_LTO_MODE=thin -DVECTORIZATION_LTO_MODE=thin -DPARALLEL_LTO_MODE=thin -DPROFILER_LTO_MODE=thin"
            extra_deps: ""
          - name: "lto-full"
            os: ubuntu-latest
            extra_cmake: "-DCORE_LTO_MODE=full -DMEMORY_LTO_MODE=full -DLOGGING_LTO_MODE=full -DVECTORIZATION_LTO_MODE=full -DPARALLEL_LTO_MODE=full -DPROFILER_LTO_MODE=full"
            extra_deps: ""
          - name: "ccache"
            os: ubuntu-latest
            extra_cmake: "-DCORE_CACHE_BACKEND=ccache -DMEMORY_CACHE_BACKEND=ccache -DLOGGING_CACHE_BACKEND=ccache -DVECTORIZATION_CACHE_BACKEND=ccache -DPARALLEL_CACHE_BACKEND=ccache -DPROFILER_CACHE_BACKEND=ccache"
            extra_deps: ""
          - name: "linker-lld"
            os: ubuntu-latest
            extra_cmake: "-DCORE_LINKER_CHOICE=lld -DMEMORY_LINKER_CHOICE=lld -DLOGGING_LINKER_CHOICE=lld -DVECTORIZATION_LINKER_CHOICE=lld -DPARALLEL_LINKER_CHOICE=lld -DPROFILER_LINKER_CHOICE=lld"
            extra_deps: ""
          - name: "memory-profiler-off"
            os: ubuntu-latest
            extra_cmake: "-DMEMORY_ENABLE_PROFILER=OFF"
            extra_deps: ""

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        submodules: recursive

    - name: Install dependencies (Ubuntu)
      if: runner.os == 'Linux'
      run: |
        chmod +x .github/workflows/install/install-deps-ubuntu.sh
        ./.github/workflows/install/install-deps-ubuntu.sh --with-tbb ${{ matrix.extra_deps }}

    - name: Install dependencies (macOS)
      if: runner.os == 'macOS'
      run: |
        chmod +x .github/workflows/install/install-deps-macos.sh
        ./.github/workflows/install/install-deps-macos.sh --with-tbb

    - name: Setup macOS linker environment
      if: runner.os == 'macOS'
      run: |
        echo "LDFLAGS=-L/opt/homebrew/opt/llvm/lib/c++ -L/opt/homebrew/opt/llvm/lib -Wl,-rpath,/opt/homebrew/opt/llvm/lib/c++ -Wl,-rpath,/opt/homebrew/opt/llvm/lib" >> $GITHUB_ENV
        echo "CPPFLAGS=-I/opt/homebrew/opt/llvm/include" >> $GITHUB_ENV
        echo "/opt/homebrew/opt/llvm/bin" >> $GITHUB_PATH

    - name: Configure
      shell: bash
      run: |
        cmake -B build \
          -C .github/cmake/configure_cache.cmake \
          -S . \
          -G Ninja \
          -DCMAKE_BUILD_TYPE=Release \
          -DCMAKE_C_COMPILER=clang \
          -DCMAKE_CXX_COMPILER=clang++ \
          -DCMAKE_CXX_STANDARD=17 \
          -DLOGGING_BACKEND=LOGURU \
          -DBUILD_TESTING=ON \
          -DCORE_ENABLE_BENCHMARK=OFF \
          -DMEMORY_ENABLE_BENCHMARK=OFF \
          -DLOGGING_ENABLE_BENCHMARK=OFF \
          -DVECTORIZATION_ENABLE_BENCHMARK=OFF \
          -DPARALLEL_ENABLE_BENCHMARK=OFF \
          -DPROFILER_ENABLE_BENCHMARK=OFF \
          -DPARALLEL_ENABLE_TBB=ON \
          -DMEMORY_ENABLE_TBB=ON \
          ${{ matrix.extra_cmake }}

    - name: Build
      run: cmake --build build -j 2

    - name: Test
      working-directory: ${{ github.workspace }}/build
      run: ctest --output-on-failure -j 2

  # ============================================================================
  # Windows Visual Studio 2022 generator + Bazel Windows build
  # ============================================================================
  windows-vs22-tests:
    name: "Windows VS2022 - C++17 Release"
    runs-on: windows-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        submodules: recursive

    - name: Install dependencies
      shell: pwsh
      run: |
        & .\.github\workflows\install\install-deps-windows.ps1

    - name: Setup MSBuild
      uses: microsoft/setup-msbuild@v2

    - name: Setup MSVC environment
      uses: ilammy/msvc-dev-cmd@v1
      with:
        arch: amd64

    - name: Configure (MSVC)
      shell: pwsh
      run: |
        $vswhere = Join-Path ${env:ProgramFiles(x86)} "Microsoft Visual Studio\Installer\vswhere.exe"
        if (-not (Test-Path $vswhere)) {
          throw "vswhere.exe not found; Visual Studio Installer is missing on this runner"
        }
        $vs17 = (& $vswhere -latest -products * -version "[17.0,18.0)" `
          -requires Microsoft.VisualStudio.Component.VC.Tools.x86.x64 `
          -property installationPath)
        if ($vs17) {
          $vsInstance = ([string]$vs17).Trim()
          Write-Host "Using Visual Studio 2022 at $vsInstance"
          cmake -B build -S . `
            -G "Visual Studio 17 2022" -A x64 `
            "-DCMAKE_GENERATOR_INSTANCE=$vsInstance" `
            -DCMAKE_CXX_STANDARD=17 `
            -DLOGGING_BACKEND=NATIVE `
            -DBUILD_TESTING=ON `
            -DPARALLEL_ENABLE_TBB=OFF `
            -DMEMORY_ENABLE_TBB=OFF `
            -DCORE_ENABLE_BENCHMARK=OFF `
            -DMEMORY_ENABLE_BENCHMARK=OFF `
            -DLOGGING_ENABLE_BENCHMARK=OFF `
            -DVECTORIZATION_ENABLE_BENCHMARK=OFF `
            -DPARALLEL_ENABLE_BENCHMARK=OFF `
            -DPROFILER_ENABLE_BENCHMARK=OFF
        } else {
          # windows-latest now ships VS 18 Enterprise; CMake's "Visual Studio 17 2022"
          # generator cannot target it. Ninja + the MSVC env from msvc-dev-cmd covers
          # the same toolchain without depending on a matching VS generator name.
          Write-Host "VS 2022 not found; configuring Ninja with the active MSVC toolchain"
          cmake -B build -S . `
            -G Ninja `
            -DCMAKE_BUILD_TYPE=Release `
            -DCMAKE_CXX_STANDARD=17 `
            -DLOGGING_BACKEND=NATIVE `
            -DBUILD_TESTING=ON `
            -DPARALLEL_ENABLE_TBB=OFF `
            -DMEMORY_ENABLE_TBB=OFF `
            -DCORE_ENABLE_BENCHMARK=OFF `
            -DMEMORY_ENABLE_BENCHMARK=OFF `
            -DLOGGING_ENABLE_BENCHMARK=OFF `
            -DVECTORIZATION_ENABLE_BENCHMARK=OFF `
            -DPARALLEL_ENABLE_BENCHMARK=OFF `
            -DPROFILER_ENABLE_BENCHMARK=OFF
        }

    - name: Build
      run: cmake --build build --config Release -j 2

    - name: Test
      working-directory: ${{ github.workspace }}/build
      run: ctest --output-on-failure --build-config Release -j 2

  bazel-windows-tests:
    name: "Bazel - Windows"
    runs-on: windows-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        submodules: recursive
        fetch-depth: 0

    - name: Setup Bazel
      uses: bazel-contrib/setup-bazel@0.8.1
      with:
        bazelisk-cache: true
        disk-cache: bazel-windows-default-${{ env.CACHE_VERSION }}
        repository-cache: true

    - name: Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.x'

    - name: Install Python dependencies
      run: |
        python -m pip install --upgrade pip
        pip install colorama==0.4.6

    - name: Bazel build + test (default config)
      working-directory: Scripts
      shell: pwsh
      run: python setup_bazel.py config.build.test

  # ============================================================================
  # Static analysis — clang-tidy (check only) + Valgrind
  # ============================================================================
  clang-tidy-tests:
    name: "clang-tidy"
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        submodules: recursive

    - name: Install dependencies
      run: |
        chmod +x .github/workflows/install/install-deps-ubuntu.sh
        ./.github/workflows/install/install-deps-ubuntu.sh --with-tbb

    - name: Configure with clang-tidy
      run: |
        cmake -B build \
          -C .github/cmake/configure_cache.cmake \
          -S . \
          -G Ninja \
          -DCMAKE_BUILD_TYPE=Debug \
          -DCMAKE_C_COMPILER=clang \
          -DCMAKE_CXX_COMPILER=clang++ \
          -DCMAKE_CXX_STANDARD=17 \
          -DLOGGING_BACKEND=NATIVE \
          -DBUILD_TESTING=ON \
          -DCORE_ENABLE_CLANGTIDY=ON \
          -DMEMORY_ENABLE_CLANGTIDY=ON \
          -DLOGGING_ENABLE_CLANGTIDY=ON \
          -DVECTORIZATION_ENABLE_CLANGTIDY=ON \
          -DPARALLEL_ENABLE_CLANGTIDY=ON \
          -DPROFILER_ENABLE_CLANGTIDY=ON \
          -DMODELS_ENABLE_CLANGTIDY=ON \
          -DCORE_ENABLE_BENCHMARK=OFF \
          -DMEMORY_ENABLE_BENCHMARK=OFF \
          -DLOGGING_ENABLE_BENCHMARK=OFF \
          -DVECTORIZATION_ENABLE_BENCHMARK=OFF \
          -DPARALLEL_ENABLE_BENCHMARK=OFF \
          -DPROFILER_ENABLE_BENCHMARK=OFF

    - name: Build (clang-tidy runs as part of compile)
      run: cmake --build build -j 2

  valgrind-tests:
    name: "Valgrind"
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        submodules: recursive

    - name: Install dependencies
      run: |
        chmod +x .github/workflows/install/install-deps-ubuntu.sh
        ./.github/workflows/install/install-deps-ubuntu.sh --with-tbb
        sudo apt-get install -y valgrind

    - name: Configure with Valgrind
      run: |
        cmake -B build \
          -C .github/cmake/configure_cache.cmake \
          -S . \
          -G Ninja \
          -DCMAKE_BUILD_TYPE=Debug \
          -DCMAKE_C_COMPILER=clang \
          -DCMAKE_CXX_COMPILER=clang++ \
          -DCMAKE_CXX_STANDARD=17 \
          -DLOGGING_BACKEND=NATIVE \
          -DBUILD_TESTING=ON \
          -DMEMORY_ENABLE_MIMALLOC=OFF \
          -DCORE_ENABLE_VALGRIND=ON \
          -DMEMORY_ENABLE_VALGRIND=ON \
          -DLOGGING_ENABLE_VALGRIND=ON \
          -DVECTORIZATION_ENABLE_VALGRIND=ON \
          -DPARALLEL_ENABLE_VALGRIND=ON \
          -DPROFILER_ENABLE_VALGRIND=ON \
          -DMODELS_ENABLE_VALGRIND=ON \
          -DCORE_ENABLE_BENCHMARK=OFF \
          -DMEMORY_ENABLE_BENCHMARK=OFF \
          -DLOGGING_ENABLE_BENCHMARK=OFF \
          -DVECTORIZATION_ENABLE_BENCHMARK=OFF \
          -DPARALLEL_ENABLE_BENCHMARK=OFF \
          -DPROFILER_ENABLE_BENCHMARK=OFF

    - name: Build
      run: cmake --build build -j 2

    - name: Test under Valgrind
      working-directory: ${{ github.workspace }}/build
      run: ctest --output-on-failure -j 1

  cppcheck-tests:
    name: "cppcheck"
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        submodules: recursive

    - name: Install dependencies
      run: |
        chmod +x .github/workflows/install/install-deps-ubuntu.sh
        ./.github/workflows/install/install-deps-ubuntu.sh --with-tbb
        sudo apt-get install -y cppcheck

    - name: Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.x'

    - name: Install Python dependencies
      run: |
        python3 -m pip install --upgrade pip
        pip install colorama==0.4.6 psutil==6.1.1

    - name: Configure, build, and run cppcheck
      working-directory: Scripts
      run: python3 setup.py config.build.cppcheck.ninja.clang

  # ============================================================================
  # Bazel feature-flag counterparts (NUMA, AVX2, GCC, MSAN).
  # SLEEF is CMake-only until Bazel grows a native SLEEF codegen path.
  # ============================================================================
  bazel-feature-flag-tests:
    name: "Bazel Feature - ${{ matrix.name }}"
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        include:
          - name: "numa"
            os: ubuntu-latest
            extra: "numa"
          # Bazel --config=sleef requires a prior CMake SLEEF build
          # (see @@sleef_cmake//:sleef_missing). Covered by CMake Feature - sleef.
          - name: "avx2"
            os: ubuntu-latest
            extra: "avx2"
          - name: "gcc"
            os: ubuntu-latest
            extra: "gcc"
          - name: "msan"
            os: ubuntu-latest
            extra: "msan"

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        submodules: recursive
        fetch-depth: 0

    - name: Setup Bazel
      uses: bazel-contrib/setup-bazel@0.8.1
      with:
        bazelisk-cache: true
        disk-cache: bazel-feature-${{ matrix.name }}-${{ env.CACHE_VERSION }}
        repository-cache: true

    - name: Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.x'

    - name: Install Python dependencies
      run: |
        python3 -m pip install --upgrade pip
        pip install colorama==0.4.6

    - name: Install NUMA headers
      if: matrix.name == 'numa'
      run: sudo apt-get update && sudo apt-get install -y libnuma-dev

    - name: Bazel build + test (${{ matrix.name }})
      working-directory: Scripts
      run: |
        if [ "${{ matrix.name }}" = "msan" ]; then
          echo "MSAN is compile-only on hosted runners (uninstrumented libstdc++)."
          python3 setup_bazel.py config.build.msan
          exit 0
        fi
        python3 setup_bazel.py config.build.test.${{ matrix.extra }}

  # ============================================================================
  # CI Success Summary
  # ============================================================================
  ci-success:
    name: CI Success
    needs:
      - build-matrix
      - tbb-specific-tests
      - sanitizer-tests
      - project-flag-backend-tests
      - vectorization-simd-backend-tests
      - optimization-flags-test
      - lto-tests
      - benchmark-tests
      - sccache-baseline-tests
      - sccache-enabled-tests
      - clang-coverage-tests
      - windows-cli-smoke
      - bazel-build-matrix
      - bazel-tbb-tests
      - bazel-project-flag-backend-tests
      - bazel-vectorization-simd-tests
      - bazel-metal-tests
      - cmake-gpu-backend-tests
      - bazel-cuda-tests
      - bazel-hip-tests
      - feature-flag-tests
      - windows-vs22-tests
      - bazel-windows-tests
      - clang-tidy-tests
      - valgrind-tests
      - cppcheck-tests
      - bazel-feature-flag-tests
    runs-on: ubuntu-latest
    if: always()

    steps:
    - name: Check all job results
      run: |
        echo "=== Streamlined CI Pipeline Results ==="
        echo "Build Matrix: ${{ needs.build-matrix.result }}"
        echo "TBB Specific Tests: ${{ needs.tbb-specific-tests.result }}"
        echo "Sanitizer Tests: ${{ needs.sanitizer-tests.result }}"
        echo "Project Flag + Backend Tests: ${{ needs.project-flag-backend-tests.result }}"
        echo "Vectorization SIMD Backends: ${{ needs.vectorization-simd-backend-tests.result }}"
        echo "Optimization Flags: ${{ needs.optimization-flags-test.result }}"
        echo "Benchmark Tests: ${{ needs.benchmark-tests.result }}"
        echo "Sccache Baseline Tests: ${{ needs.sccache-baseline-tests.result }}"
        echo "Sccache Enabled Tests: ${{ needs.sccache-enabled-tests.result }}"
        echo "Clang Coverage Tests: ${{ needs.clang-coverage-tests.result }}"
        echo "Windows CLI Smoke: ${{ needs.windows-cli-smoke.result }}"
        echo "Bazel Build Matrix: ${{ needs.bazel-build-matrix.result }}"
        echo "Bazel TBB Tests: ${{ needs.bazel-tbb-tests.result }}"
        echo "Bazel Project Flag + Backend Tests: ${{ needs.bazel-project-flag-backend-tests.result }}"
        echo "Bazel Vectorization SIMD Tests: ${{ needs.bazel-vectorization-simd-tests.result }}"
        echo "Bazel Metal Tests: ${{ needs.bazel-metal-tests.result }}"
        echo "CMake GPU Backends: ${{ needs.cmake-gpu-backend-tests.result }}"
        echo "Bazel CUDA Tests: ${{ needs.bazel-cuda-tests.result }}"
        echo "Bazel HIP Tests: ${{ needs.bazel-hip-tests.result }}"
        echo "Feature Flag Tests: ${{ needs.feature-flag-tests.result }}"
        echo "Windows VS2022: ${{ needs.windows-vs22-tests.result }}"
        echo "Bazel Windows: ${{ needs.bazel-windows-tests.result }}"
        echo "clang-tidy: ${{ needs.clang-tidy-tests.result }}"
        echo "Valgrind: ${{ needs.valgrind-tests.result }}"
        echo "cppcheck: ${{ needs.cppcheck-tests.result }}"
        echo "Bazel Feature Flags: ${{ needs.bazel-feature-flag-tests.result }}"
        echo ""

        # Check if any required job failed
        if [ "${{ needs.build-matrix.result }}" != "success" ]; then
          echo "ERROR: Build matrix failed"
          exit 1
        fi
        if [ "${{ needs.tbb-specific-tests.result }}" != "success" ]; then
          echo "ERROR: TBB specific tests failed"
          exit 1
        fi
        if [ "${{ needs.sanitizer-tests.result }}" != "success" ]; then
          echo "ERROR: Sanitizer tests failed"
          exit 1
        fi
        if [ "${{ needs.project-flag-backend-tests.result }}" != "success" ]; then
          echo "ERROR: Project flag + backend tests failed"
          exit 1
        fi
        if [ "${{ needs.vectorization-simd-backend-tests.result }}" != "success" ]; then
          echo "ERROR: Vectorization SIMD backend tests failed"
          exit 1
        fi
        if [ "${{ needs.optimization-flags-test.result }}" != "success" ]; then
          echo "ERROR: Optimization flags test failed"
          exit 1
        fi
        if [ "${{ needs.benchmark-tests.result }}" != "success" ]; then
          echo "WARNING: Benchmark tests failed (non-blocking)"
        fi
        if [ "${{ needs.sccache-baseline-tests.result }}" != "success" ]; then
          echo "ERROR: Sccache baseline tests failed"
          exit 1
        fi
        if [ "${{ needs.sccache-enabled-tests.result }}" != "success" ]; then
          echo "ERROR: Sccache enabled tests failed"
          exit 1
        fi
        if [ "${{ needs.clang-coverage-tests.result }}" != "success" ]; then
          echo "ERROR: Clang coverage tests failed"
          exit 1
        fi
        if [ "${{ needs.bazel-build-matrix.result }}" != "success" ]; then
          echo "ERROR: Bazel build matrix failed"
          exit 1
        fi
        if [ "${{ needs.bazel-tbb-tests.result }}" != "success" ]; then
          echo "ERROR: Bazel TBB tests failed"
          exit 1
        fi
        if [ "${{ needs.bazel-project-flag-backend-tests.result }}" != "success" ]; then
          echo "ERROR: Bazel project flag + backend tests failed"
          exit 1
        fi
        if [ "${{ needs.bazel-vectorization-simd-tests.result }}" != "success" ]; then
          echo "ERROR: Bazel Vectorization SIMD tests failed"
          exit 1
        fi
        if [ "${{ needs.bazel-metal-tests.result }}" != "success" ]; then
          echo "ERROR: Bazel Metal tests failed"
          exit 1
        fi
        if [ "${{ needs.cmake-gpu-backend-tests.result }}" != "success" ]; then
          echo "ERROR: CMake GPU backend tests failed"
          exit 1
        fi
        if [ "${{ needs.bazel-cuda-tests.result }}" != "success" ]; then
          echo "ERROR: Bazel CUDA tests failed"
          exit 1
        fi
        if [ "${{ needs.bazel-hip-tests.result }}" != "success" ]; then
          echo "ERROR: Bazel HIP tests failed"
          exit 1
        fi
        if [ "${{ needs.feature-flag-tests.result }}" != "success" ]; then
          echo "ERROR: Feature flag tests failed"
          exit 1
        fi
        if [ "${{ needs.windows-vs22-tests.result }}" != "success" ]; then
          echo "ERROR: Windows VS2022 tests failed"
          exit 1
        fi
        if [ "${{ needs.bazel-windows-tests.result }}" != "success" ]; then
          echo "ERROR: Bazel Windows tests failed"
          exit 1
        fi
        if [ "${{ needs.clang-tidy-tests.result }}" != "success" ]; then
          echo "ERROR: clang-tidy tests failed"
          exit 1
        fi
        if [ "${{ needs.valgrind-tests.result }}" != "success" ]; then
          echo "ERROR: Valgrind tests failed"
          exit 1
        fi
        if [ "${{ needs.cppcheck-tests.result }}" != "success" ]; then
          echo "ERROR: cppcheck tests failed"
          exit 1
        fi
        if [ "${{ needs.bazel-feature-flag-tests.result }}" != "success" ]; then
          echo "ERROR: Bazel feature flag tests failed"
          exit 1
        fi

        echo ""
        echo "SUCCESS: All essential CI checks passed successfully!"

# ============================================================================
# MAINTENANCE GUIDE FOR FUTURE DEVELOPERS
# ============================================================================
#
# This CI workflow has been streamlined for efficiency while maintaining comprehensive
# test coverage. Here's what you need to know:
#
# ## Job Structure Overview:
# 1. build-matrix: Primary testing across platforms with C++17 baseline + selective C++20/C++23
# 2. compiler-version-tests-clang: Clang version testing (Clang 9, 18, 20, 21 with C++17/20/23)
# 3. compiler-version-tests-gcc: GCC version testing (GCC 8-13 with C++17/20/23)
# 4. tbb-specific-tests: TBB functionality testing (Unix/macOS only)
# 4. sanitizer-tests: Memory/thread safety testing (Unix/macOS only)
# 5. vectorization-simd-backend-tests: VECTORIZATION_CPU_BACKEND sse/avx512 on ubuntu-latest (avx512 tests run only if the CPU has avx512f); neon/sve on ubuntu-24.04-arm
# 6. optimization-flags-test: Compiler optimization testing
# 7. benchmark-tests: Performance regression testing (non-blocking)
# 8. sccache-baseline-tests: Build performance baseline without sccache (all platforms)
# 9. sccache-enabled-tests: Build performance with sccache enabled (all platforms)
# 10. bazel-build-matrix: Bazel build+test via Scripts/setup_bazel.py, default config
#     (Ubuntu + macOS; Windows Bazel is smoke-tested only, see windows-cli-smoke)
# 11. bazel-tbb-tests: Bazel build+test with --config=tbb (Ubuntu + macOS)
# 12. bazel-project-flag-backend-tests: Bazel project/backend matrix, mirrors
#     project-flag-backend-tests via --project.NAME (Ubuntu only; no Bazel
#     equivalent for the XSIGMA_ENABLE_EXTERNAL dimension)
# 13. bazel-vectorization-simd-tests: Bazel Vectorization SIMD tier matrix, mirrors
#     vectorization-simd-backend-tests but through Bazel (--define vectorization_type=X)
# 14. bazel-metal-tests: Bazel build+test with Metal GPU backend (macOS only, real GPU)
# 15. ci-success: Aggregates results from all jobs
#
# ## Bazel Jobs:
# - All five Bazel jobs run through Scripts/setup_bazel.py (never `bazel` directly),
#   mirroring the CMake jobs' use of Scripts/setup.py.
# - bazel-contrib/setup-bazel@0.8.1 installs bazelisk (reading .bazelversion) and
#   provides disk/repository caching.
# - Windows is not yet covered by a real Bazel build/test job — only a
#   `setup_bazel.py --help` smoke test in windows-cli-smoke. The Bazel setup has not
#   been verified end-to-end on Windows; add a real Windows job once it has been.
# - See Docs/BAZEL_USER_GUIDE.md's "Known Gaps and CMake/Bazel Alignment Plan" section
#   for the bug classes these jobs specifically guard against (TBB linkage, the
#   aarch64 select() ambiguity, Metal wiring).
#
# ## Platform-Specific Notes:
# - Ubuntu: Full TBB + CUDA support, all sanitizers including leak
# - macOS: Full TBB support, CUDA not supported on Apple Silicon, limited sanitizers
# - Windows: No TBB (tested separately with Visual Studio), CUDA support via Chocolatey
#
# ## Adding New Test Configurations:
# To add a new test configuration to build-matrix:
# 1. Add a new entry to the matrix.include section
# 2. Follow the naming convention: "Platform C++XX BuildType - Backend - Feature:State"
# 3. Set appropriate values for: os, build_type, cxx_std, logging_backend, tbb_enabled, cuda_enabled
# 4. Add platform-specific compiler and cache path settings
#
# ## Dependency Installation Scripts:
# All dependency installation has been refactored into separate scripts for maintainability:
# - .github/workflows/install/install-deps-ubuntu.sh: Ubuntu/Linux dependencies (with --with-tbb, --with-cuda flags)
# - .github/workflows/install/install-deps-macos.sh: macOS dependencies (with --with-tbb, --with-cuda flags)
# - .github/workflows/install/install-deps-windows.ps1: Windows dependencies (PowerShell, with -WithTbb, -WithCuda flags)
# - .github/workflows/install/install-sccache.sh: Sccache installation with platform detection
#
# To modify dependencies:
# 1. Update the appropriate script in .github/workflows/install/
# 2. Scripts are idempotent and include error handling
# 3. Test changes locally before committing
#
# ## Build System Integration:
# - All builds now use setup.py from Scripts/ directory
# - setup.py handles CMake configuration and build execution
# - Supports multiple compiler versions and C++ standards
# - Automatically detects and uses build_ninja_python directory if it exists
#
# ## Adding New Jobs:
# 1. Create the new job following the existing pattern
# 2. Add the job name to the ci-success.needs list
# 3. Add result checking logic in the ci-success job
#
# ## Cache Management:
# - Cache keys use CACHE_VERSION (currently v3) - increment when dependencies change significantly
# - Each job type has its own cache namespace to avoid conflicts
#
# ## Troubleshooting Common Issues:
# - CUDA installation failures are non-fatal and logged as warnings
# - TBB linkage verification may show "statically linked" - this is normal
# - Sanitizer tests are restricted to Unix systems for better compatibility
# - Windows TBB testing is intentionally excluded (handled in separate VS pipeline)
#
# ## Performance Considerations:
# - Matrix size has been optimized to balance coverage vs. CI time
# - C++20/C++23 testing is selective rather than exhaustive
# - Benchmark tests are non-blocking to prevent CI delays
# - Code quality and coverage jobs have been removed for faster execution
#
# ## Compiler Version Matrix Testing:
# - compiler-version-tests-clang: Tests Clang versions (9, 18, 20, 21) with C++17/20/23
# - compiler-version-tests-gcc: Tests GCC versions (8-13) with C++17/20/23
# - Each job installs only one compiler version to avoid package conflicts
# - Each compiler version tests with TBB enabled and LOGURU logging backend
# - Non-critical jobs (warnings only) - allows CI to pass even if specific compiler versions fail
# - To add new compiler versions:
#   1. Add new matrix entry to compiler-version-tests-clang or compiler-version-tests-gcc job
#   2. Specify compiler_c, compiler_cxx, compiler_name, compiler_version
#   3. Set appropriate cxx_std and build_type
#   4. Ensure compiler is available in the target platform
#
# ## Sccache Performance Testing:
# - sccache-baseline-tests: Builds without sccache for baseline metrics
# - sccache-enabled-tests: Builds with sccache enabled via CMAKE_CXX_COMPILER_LAUNCHER
# - Both jobs run on Ubuntu, macOS, and Windows with identical configurations
# - Sccache version is controlled via SCCACHE_VERSION environment variable
# - Sccache cache directory is cached between runs for better performance
# - Build duration is recorded and reported for performance comparison
# - To update sccache version, modify SCCACHE_VERSION in the env section
# - Sccache stats are displayed after each build for debugging
#
# ## Security Notes:
# - All external downloads (CUDA, Intel repos) include error handling
# - Package installations use official repositories where possible
# - Chocolatey installations include retry logic for transient failures
# - Sccache downloads are from official Mozilla GitHub releases
